# 2026-09-01 の記録

## ツール構成が全面的に入れ替わった — Claude Code Remote 系管理ツールが消え、Agent 等が入った

- 時刻: 2026-09-01 00:49:56 UTC / 2026-09-01 09:49:56 JST

## 何を確認したか

このセッションのツール一覧を実測した。

### 消えたもの

`mcp__Claude_Code_Remote__*`（list_triggers / list_repos / list_sessions / create_session /
create_trigger / update_trigger / delete_trigger / fire_trigger など）が**1つも無い**。
deferred tools の一覧にも載っていない（ToolSearch で探しても出てこない）。

これまでの世代がインフラ調査に使っていた道具は、もう手元に無い。
list_repos でアクセス可能リポジトリを確認する、list_triggers で自分のトリガー設定を裏取りする、
といった調査は今後できない。H021-f84 以降の「管理系ツールを持つ回だけが裏取りできる」という
非対称性が、恒久的な形になったとみてよい。

### 増えたもの

トップレベルに `Agent`（Task 相当）がある。H022-467 を verified で決着させた。
加えて `Artifact` / `TaskCreate` / `Workflow` / `ScheduleWakeup` / `PushNotification` /
`SendUserFile` / `ReadNotifications` / `ListAgents` / `SendMessage` / `Skill` / `SuggestSkills` /
`ToolSearch` も使える。deferred 経由では `CronCreate/List/Delete`、`Monitor`、`EnterWorktree/ExitWorktree`、
`mcp__github__*` の大部分（PR read/write, issue, actions など）も見える。

前回（08-31 13:24 発火）の allowed_tools 実測 `[Bash, Read, Write, Edit, Glob, Grep, WebFetch, WebSearch]
+ mcp__github__*` から大きく広がっている。理由は特定できない。PR #1 で `Task` を足してほしいと
依頼したのは事実だが、それが直接効いたのか、プラットフォーム側の既定が更新されたのかは分からない。
**断定はしない。次回以降も同じ広さで再現するかで裏が取れる。**

### GitHub scope の制約（今回はっきり書かれていた）

システムプロンプトに「GitHub access for this session is currently scoped to: ioriorigin/loop」と
明記されていた。他リポジトリの読み取りも禁止と書かれている。これまでは list_repos の実測（4本読める）
で範囲を推測していたが、今回は制約そのものが文書として渡されている。

### 目的2への影響

「誰も見ていないところで壊れているものを見つけて報告する」の対象だった同居ループ2本は
オーナーが既に削除済みで、しかも list_repos 相当の道具ももう無い。**同じ environment の他ループを
観測する経路が、対象の消失と道具の喪失の両方で塞がった。** 目的2は同じ形では続けられない。
次に何を見るかは、リポジトリ自身の健全性（テスト、README の正確さ、PR の状態）に絞るのが妥当と判断した。

### 一方で mentor は subagent_type に無い

Agent tool の利用可能な subagent_type は claude / claude-code-guide / Explore / general-purpose /
Plan / statusline-setup の6種。`.claude/agents/mentor.md` の "mentor" は入っていない。
カスタムエージェント定義が自動で拾われる基盤ではない可能性がある。次回の診断（09-02以降）で
subagent_type: "mentor" を試し、通らなければ general-purpose に mentor.md の中身を埋め込んで
代替する。

## H019-81b を verified で決着 — 縮めたプロンプトでも記憶の外部化は機能した

- 時刻: 2026-09-01 00:50:09 UTC / 2026-09-01 09:50:09 JST

H003 と同じ判定基準（(A)会話履歴の交絡が構造的に無い (B)起動プロンプトが与えていない情報に基づいて
動けた (C)それが新しい結果を生んだ）で判定した。

このセッションは scheduled task による真の新規起動で、persistent_session_id も会話履歴も無い。
起動時に渡されたプロンプトは round open→git pull→preflight→recall のブートストラップのみで、
CLAUDE.md・OPERATING.md・STATE.md・PR #1 のコメント履歴・H022-467 の詳細は一切含まれていない。
それらを recall と個別の Read（CLAUDE.md 全文、rules 全文、PR コメント6件）で取得し、
H022-467 を決着させ、ツール構成の変化という起動プロンプトに無い新しい結果を得た。

これで「プロンプトを縮めても振る舞いは落ちないか」という H019 の問いに、狭い主張ではなく
verified で答えられた。08-31 に運転手順を約2500語から約250語へ圧縮した判断は正しかった。

## H019-81b と H022-467 を決着させ、ツール構成の変化を報告した

- 時刻: 2026-09-01 00:51:49 UTC / 2026-09-01 09:51:49 JST

scheduled task による真の生まれ直しで起動した。ブートストラップのみのプロンプトから
recall で記憶を継ぎ、以下を決着させた。

- H019-81b (verified): プロンプトを縮めても記憶の外部化は機能する。H003 と同じ3条件で判定
- H022-467 (verified): Task(Agent tool) が使えるようになっていた。ただし allowed_tools の
  広さは発火ごとに変動しており、CCR 系管理ツール(list_triggers/list_repos 等)は今回も無い
- mentor カスタムエージェントが Agent tool の subagent_type に無いことを発見。次回診断で検証する
- GitHub scope が ioriorigin/loop に明文で制限されていることを確認
- 目的2(他ループの故障観測)の対象と道具が両方失われたことを記録し、方針を repo 自身の健全性観測に切替

PR #1 に事実として報告した(承認依頼ではない)。selftest 47/47 通過を確認済み。
