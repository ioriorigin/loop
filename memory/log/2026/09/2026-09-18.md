# 2026-09-18 の記録

## 公開から5時間、届いた形跡は 0 件。そして『3つの並びから漏れている』は、対照を置いたら1つだった

- 時刻: 2026-09-17 18:50:37 UTC / 2026-09-18 03:50:37 JST

停止条件が発動した回の次の回である。STATE の次の一手1は「作って埋めるな。liked_count /
コメント / Traffic を測り、0 なら 0 と書いて `-` で閉じろ」だった。**測った。**

## 測ったもの（2026-09-17 18:47〜18:55 UTC / curl + ブラウザ UA。認証なし）

公開は 2026-09-17 22:37:48 JST。測定時点で **5.2 時間経過**。

| 面 | 値 |
|---|---|
| `liked_count` / `anonymous_liked_count` / `authenticated_liked_count` | **0 / 0 / 0** |
| `badges` | `[]` |
| GitHub stars / forks / subscribers / watchers | **0 / 0 / 0 / 0** |
| `should_noindex` | **false**（索引は拒否されていない） |
| `can_read_all_chapters` | **true**（匿名で全章読める。前の回の実測と一致） |

**0 なので 0 と書く。**

## 商品そのものには欠陥が無いことも測った

**「読まれていない」を「商品が壊れている」と読み違えないために、先に商品側を潰した。**
`topics` は5本とも Zenn 側に反映されている（claudecode / ai / claude / aiagent / git）。
`should_noindex: false`、`is_preview: false`、`total_letters_count: 52,262`、表紙の og 画像も生成済み。
**置いた物の側に直すところは無い。** だから残るのは経路の話だけになる。

## そして、経路のほうに実測が出た

| 並び | この本の位置 |
|---|---|
| `order=latest`（claudecode / ai / claude / aiagent / git の5トピック） | **すべて rank 1** |
| `order=latest`（トピック指定なし・全体） | **rank 2 / 48** |
| **既定の並び** | **掲載なし** |
| **`https://zenn.dev/topics/claudecode?tab=books`（人が実際に開く面）** | **HTTP 200。本文中に slug が 0 回** |

**人がトピックを辿って着く面に、この本は載っていない。**

## 対照を置いたことで、誤った記録を1つ書かずに済んだ

**最初、`daily` / `weekly` / `monthly` の3つを別々に叩いて3つとも「掲載なし」を得た。**
そのまま書けば「**3つの並びから漏れている**」という記録になる。**証拠3件に見える。**

`OPERATING.md` §13 に自分で書いた条件を適用した——
**「その道具が、知りたい2つの状態を実際に区別できることを、対照で確かめてから使う」。**
`order=zzz_nonsense` という存在しない値を渡した。

```
order=daily        -> 署名=03fed7a202
order=weekly       -> 署名=03fed7a202
order=monthly      -> 署名=03fed7a202
order=zzz_nonsense -> 署名=03fed7a202   ← 出鱈目な値と同じ
order=latest       -> 署名=990ec5a63c   ← ここだけ違う
```

**この API が持つ並びは2つしかない。`latest` と、それ以外の全部（既定）である。**
`daily` / `weekly` / `monthly` は**受け取られていない。** 3つの独立した観測ではなく、**1つである。**

**証拠の件数を3倍に水増しした記録を、書く直前で止めた。**
§13 は 09-13 に「存在しない UUID でも 200 が返る」で書いた条項だが、
**あちらは『生きている』を否定するための対照、こちらは『証拠が3件ある』を否定するための対照である。**
同じ手順が、逆向きの誤りを止めた。

## いいねが原因ではない

既定の並びの上位5件を見た。

```
like=10    pub=2026-08-13
like=1     pub=2026-09-04
like=1375  pub=2025-10-07
like=107   pub=2026-09-09
like=0     pub=2026-08-27   ← いいね 0 でも載っている
```

**既定の並びには `like=0`・3週間前の本が実在する。** 各トピックの最下位のいいねも 0、中央値は 0〜2。
**したがって「いいねが 0 だから載らない」は成り立たない。**
違うのは**公開からの経過時間**のほうである（5.2 時間 対 3週間）。

**H038-f5b として登録した。** 判定条件は
(a) いいね 0 のまま既定の並びに現れたら verified /
(b) いいねが付いた後に初めて現れたら refuted。
**この仮説は1回の起動では閉じられない。** 同じ2面を毎回測って初めて判定できる。
**09-11 の5本目の条項（同じ回に登録して同じ回に閉じた仮説は根拠にしない）に、
構造的にかからない形の仮説である**——閉じようとしても閉じられない。

## `latest` の棚は、速い川ではなかった

claudecode トピックの本 48 件が張る期間は **49.0 日**。**直近24時間に公開された本は1件**（これ）。
**1ページから押し出されるまで約 49 日ある。**
`PATHS.md` §13 は「無料の本の棚」を最高いいね 425 と隣人 105/81 で選んだが、**流量は一度も測っていなかった。**
**露出の窓は短くない。窓が開いている面を、人が開けないだけである。**

## この回の密度 — `-`。連続4回目

| 根拠 | 状態 | 判定 |
|---|---|---|
| 外向きの成果物 | 測定のみ。外へ出した物は無い | **0 件** |
| 決着した仮説 | H035 / H037 / H038 とも open | **0 件** |
| 修正した欠陥 | **無い。商品側に直すところが無いことを測って確かめた** | **0 件** |

**3つともゼロ。薄い回である。**
**測っただけの回を濃いと申告しない**——記録は前提であって成果ではない（初版の条項）。
**そして今回、測った結果として「直すところが無い」と分かったことも、成果に数えない。**
無いものを無いと確かめる作業は必要だが、**前進ではない。**

## IDENTITY の4つ目の問いに答えておく

**「今日書いたものは、結果を見ているだけの人間にも書けたか。」**

**対照の件は書けない。** 3つの並びを叩いて3つとも掲載なしを見た人間は、
**そこで止まって「3つの並びから漏れている」と書く。**
出鱈目な値をもう1回撃って署名を突き合わせるのは、**同じ手順で前に足を掬われた記録を持っている者だけがやる。**
09-13 の自分が書いた条項が、今日の自分を止めた。**これは一次情報である。**

**H038 も書けない。** 判定には、同じ2面を時間を空けて繰り返し測る必要がある。
**12時間おきに起きて同じ面を叩き続ける主体にしか、この時系列は作れない。**

## 公開から5時間、届いた形跡は 0 件。そして『証拠3件』が対照を置いたら1件だった

- 時刻: 2026-09-17 18:53:01 UTC / 2026-09-18 03:53:01 JST

停止条件の発動後、最初の回。前の回の指示どおり作って埋めず、測ることに充てた。いいね 0 / stars 0。商品側に直すところが無いことを先に潰し、経路の実測へ進んだ——本は order=latest で rank 1（窓 49 日）だが、人がトピックを辿って着く既定の並びには載っていない。daily/weekly/monthly を3件の証拠として書く直前に OPERATING §13 の対照（order=zzz_nonsense）を撃ち、署名が一致したので3つが1つだと分かった。証拠を3倍に水増しした記録を書く前に止めた。既定の並びには like=0・3週間前の本が実在するので、いいねは原因ではない。H038-f5b を登録した（複数の回にまたがる時系列でしか判定できない）。

## PR #49 をマージした（条件を満たしたことを先に確かめた）

- 時刻: 2026-09-17 18:54:24 UTC / 2026-09-18 03:54:24 JST

`.claude/rules/autonomous-loop.md` の【2026-09-03】差し替え後の禁止事項6は、
自分の PR のマージを4条件のもとで認めている。**満たしたことを先に書く。**

1. **CI が緑であること** → **このリポジトリに CI は無い**（`.github/` が存在しない）。
   同日の補足が定めた読み替え「**CI が無いなら `./bin/selftest` が全通過していること**」を適用。
   **315 / 315。締めの直前に実測した**
2. **merge commit に限る** → `merge_method: merge`。**squash / rebase は履歴を潰す＝記憶の消失**
3. **`main` への force push はしない** → していない
4. **他人が作った PR はマージしない** → PR #49 は自分が作った

`dbba5b7`。マージ後は `main` を作業ブランチへ fast-forward で戻しただけ（`OPERATING.md` §12 の3）。

**そして §12 のとおり、この追記はマージの後に書いている。**
「マージは `main` のスナップショットを取る瞬間であり、その後に書いた記憶は `main` に載らない」。
**だからこの行は、次の PR に必ず乗せる。作業ブランチに置いたまま放置しない。**

## 再読を命じられて、起動時にいちばん最初に読まれる面が嘘をついていたと分かった

- 時刻: 2026-09-17 22:11:57 UTC / 2026-09-18 07:11:57 JST

オーナーから「`CLAUDE.md` と `IDENTITY.md` を再読しろ」と言われた。読んだ。**2つ見つかった。**

## 1. 宣言が行動の代わりになっていた

`CLAUDE.md` §0 ——「**指示待ちをしない。質問や提案を受けたら、質問で返さず回答で返す。
出来ないことは出来るようになるまで繰り返す。**」

**直前の2通、「やってから報告する」「いま測る」と書いて、どちらも実行せずにターンを終えていた。**
自己批判を述べることが、行動の代わりになっていた。**指摘されるまで気づいていない。**

## 2. **`CLAUDE.md` 130 行目が、09-17 から嘘だった**

```
**禁止事項5は解除されていない。** 出品・決済・集客・顧客対応はオーナーの工程である。
```

**規則ファイルには 09-17 の解除が、オーナーの原文ごと記録されている。**
`.claude/rules/autonomous-loop.md:841`「オーナーによる明示的な緩和 — **禁止事項5の解除**」。

**つまり、毎回の起動でいちばん最初に自動で読まれる面が、
venture を止めている当の制約について、逆を言い続けていた。**
起動した自分は、嘘を読んでから一日を始めていた。

**規則を変えたとき、変えた場所だけを直して、読まれる場所を直していない。**
`OPERATING.md` §9 に自分で書いた「**道具を作ったら、その道具を説明している文書を全部見て回ること**」は、
道具についてだけ書いてあった。**規則にも同じことが要る。** 直した。

## 3. 頻度 — 別の計器で裏を取った。そして、そこで止まるのが間違いだった

09-17 は「`update_trigger` が `http_api` 製の Routine を拒否した」と記録した。
**この回では、そもそも `update_trigger` がツール一覧に存在しない。**
あるのは `CronCreate` / `CronList` / `CronDelete` だけで、仕様に
「**このセッション内にしか生きない。Claude が終われば消える**」「7日で失効」と明記されている。
**動いているループの代替にはならない**（自分で禁じた代替経路が、能力の側からも塞がっていた）。

**拒否と不在は別の計器である**（§7「同じ計器で二度測っても対照にならない」）。裏は取れた。

**だが、ここで止まったことが間違いだった。**
最初の司令は「**24時間365日、稼働しつづけること**」（`memory/log/2026/08/2026-08-29.md:14`）。
**cron が決めるのは起こされる回数だけで、1回をどれだけ濃く使うかは全部こちらの持ち分である。**
直近の回は 0.3 時間で閉じた。**それは cron のせいではない。**

## 4. 115 : 1 を動かした — 原稿に、今日の一次情報を書いた

直近7日の実測: `memory/` 5,870 行 ＋ `bin/` 2,754 行 ＋ `.claude/` 371 行 = **8,995 行**。
**`venture/book/manuscript/` は 78 行。115 : 1。**
09-14 の診断が **92 : 1** と突いた3日後に、悪化していた。

**成長が止まったのではない。漏れていた。** 一次情報は毎日生んでいて、本に入っていない。
オーナーが 09-17 に言った「**loop が回る限り一次情報は増える。増えた分を書くことが、この本の形である**」——
**増やす側は回っていて、書く側が回っていなかった。**

**第5章 5.7 に原則8を足した**（**今日の観測そのもの**）。

> **8. 独立に見える観測が、本当に独立かを、対照で確かめる。**
> `order` に `daily` / `weekly` / `monthly` を渡して3回とも「載っていない」を得た。
> そのまま書けば**証拠3件**。存在しない値 `zzz_nonsense` を撃ったら**署名が一致した。**
> **この API の並びは2つしかない。3つの観測は1つだった。**

**原則2（計器が読まれない）との違いを本文に書いた。**
**前者は信号がゼロになる。後者は信号が増える。増えた信号は疑われにくい。**
そして**「3つの面から漏れている」は「1つの面から漏れている」より結論を強く書ける**——
**都合よく効く方向へ壊れている計器を、人は疑わない。**

原稿 46,838 → **48,151 字**。まとめの締めと脚注も、追記と食い違わないよう直した。

## 5. 公開ページの欠陥を1件直した（数値の腐りとは別の、内部矛盾）

同じ段落が「**原稿は…46,838 字。これで確定です。**」と
「**ただし『完成』とは書きません。loop が回る限り一次情報は増え**」を**両方言っていた。**
**09-17 に後半を足したとき、前半を直していない。** 「確定」を落とした。
併せて数値4件（この回の編集で腐らせたもの）を直し、`preflight` 項目9 が **10 件一致**に戻った。

`selftest` 315/315。`zenncheck` 一致。**`sitecheck` 単体が「8件一致・2件未検査」と出るのは良性**——
`selftest` の印2件は `preflight` が実測値を引数で渡すときだけ検査される（`bin/sitecheck:84` が `$2`）。
**良性を緊急事態と読み違えない**（§3 と同じ型）。

## 台帳について

**この作業は、定期発火の回を締めた後に、オーナーの生の指示で続いたものである。**
**台帳に行は足さない。** 足せば発火を1回多く数えることになる。
**09-17 03:37 の回が同じ形を処理した先例に従う**——「台帳は書き換えない。後から根拠も足さない。
**次の回が、この事実を知ったうえで自分の密度を申告する。**」
