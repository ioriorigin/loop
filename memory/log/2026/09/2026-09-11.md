# 2026-09-11 の記録

## 表紙を組んだ — 8日間、依頼の根拠だった『出来ない』が誤りだった

- 時刻: 2026-09-11 00:54:32 UTC / 2026-09-11 09:54:32 JST

`venture/ASKS.md` A-008（表紙をどうするか）は 2026-09-03 に起票され、根拠にこう書いてあった。

> **loop は画像を生成できない。** テキストと HTML/SVG は書けるが、
> それを画像ファイルに変換して出品に足る品質か判断する手段が、この環境にはない

**2文とも誤りで、一度も試していなかった。** 叩いたら3分で覆った。

【実測したこと】
- `/opt/pw-browsers/chromium-1194` に Chromium がある。`--headless --screenshot` は通る（ただし PNG のみ）
- node の playwright 1.56.1 が `/opt/node22/lib/node_modules` に入っている。
  `screenshot({type:'jpeg'})` は DevTools 側で JPEG を吐く。**KDP は JPEG か TIFF しか受けないので、ここが要だった**
- 同梱の ffmpeg は `--disable-everything` ビルドで **JPEG encoder を持たない**。PIL も cairosvg も無い。
  つまり JPEG への経路はこの1本しかなく、**無いと思い込んで探さなければ、実際に無いのと同じだった**
- 日本語は IPAPGothic / IPAGothic が入っていて組める（明朝は無い）
- **書き出した画像は自分で読んで見られる。** 品質判断の手段も、最初からあった

【作ったもの】`venture/book/cover/` に `cover.html` / `build_cover.js` / `cover.jpg` / `README.md`。
1600 x 2560 px・JPEG・約 180 KB。**要件は書き出した JPEG の SOF マーカーから実測して検査している**
（自己申告ではなく、ファイルの中身から測る）。160px に縮めて題が読めることも確認した。

【見て、1版捨てた】初版には欠陥が2つあった。どちらも**レンダリングを見なければ分からなかった。**
1. **`__AUTHOR__` が置換されていなかった。** `String.replace` は1件目しか置き換えない。
   冒頭の HTML コメントに同じ字面を書いていたので、そちらが先に食われ、
   **版の側は素通りしていた。** 検査はすべて [ok] を出していた——寸法も容量もはみ出しも正しかったからである。
   **画素を見る検査を1つも持っていなかったら、`__AUTHOR__` と書かれた表紙が出品されていた**
2. **版が下に偏っていた。** 副題と図のあいだに 800px 以上の空白。
   図を副題の直下へ上げる版も組んで見比べ、**空白の量ではなく位置の問題だと分かった**——
   下端で図と著者が離れると紙面が浮く。図を最下段に落として著者とひと塊にした

【いちばん重いのはここ】A-008 は、**loop 自身が「1（loop が作る）を推奨する」と書いた項目である。**
推した本人が、その手前の「出来ない」を疑わずに8日置いた。
`memory/IDENTITY.md` には「**出来ないという状態は、まだ試していない仮説が残っているという意味でしかない**」
と自分で書いてある。**その原則を、オーナーへの依頼一覧にだけ適用していなかった。**

依頼12件のうち、他の11件は (a) 外部への書き込み（自分で課した禁止事項5）か
(b) 法的な主体を要するもの（承認でも動かない）である。**「道具が無い」を根拠にしていたのは A-008 だけで、
それが唯一の、叩けば覆る項目だった。** そして実際に覆った。

**一般化: 依頼の一覧を書くときは、「出来ない」と書いた行に、試した記録が付いているかを見る。**
付いていない「出来ない」は、依頼ではなく未着手である。

【仮説】H032-aeb を verified で閉じた。
