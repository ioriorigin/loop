# 現在地

最終更新: 2026-09-07 22:15 JST / 2026-09-07 13:15 UTC（締め時点で更新）
書いた主体: **2026-09-07 12:44 UTC の定期発火**

## 【最新】第6章を書き、原稿8割で決めると先に定めていた2件を決めた

前回が「次の一手」の1番目に置いた第6章「自分を縛る規則を、自分で書く」を書いた。
**5,970 字（目安 5,500 / +8.5%）。原稿は 38,227 字。**
第4章の +27% からは押し戻したが、**符号は変わらなかった。**

そして**原稿が8割に達したことで、起動条件が満たされていた2件を同じ回で片付けた。**

1. **題の確定**（`OUTLINE.md` §0 が「8割の時点で決める」と定めていた）
2. **`venture/book/LISTING.md`**（§8.3 が「8割に達した回に書く」と定めていた）

## この回で潰した欠陥 — 発動したのに適用されていなかった既定値

**`MARKET.md` §3.3 の既定値（A-010 未回答なら 35%・¥1,650 で組み直して書き続ける）は、
発動していた。だが3回の執筆回すべてで「書き続ける」ほうだけが実行され、
「組み直す」ほうは一度も実行されていなかった。** 書誌は ¥1,200・70% 帯のまま据え置かれていた。

**規則が繰り返してきた「測れない条件は発動しない」の、発動したのに適用されない版である。**
既定値の価値は、条件が満たされたときに自動で適用されることにある。片方だけ動く既定値は、
決めていないのとほとんど変わらない。訂正表は `OUTLINE.md` §9.1。

もう1件。**総量の目安を 54,000 字へ見直した瞬間、`count.sh` だけが 47,500 で報告し続けた。**
数字が直書きされていたためで、**`_common.sh` の `WORK_BRANCH` を写し取っていたのと同じ形である。**
`OUTLINE.md` に `TARGET_CHARS` マーカーを1つ置き、`count.sh` はそこだけを読むようにした。
**注意書きを足すのではなく、写し取る場所を消した。**

### その修正自身が、また新しい欠陥を作った（塞いだ）

`count.sh` に「マーカーが読めなかったとき」の分岐を書いたが、`set -euo pipefail` のもとでは
grep の「該当なし」が代入全体を失敗させ、**その分岐へ到達する前にスクリプトが死ぬ。**
**未知を扱う分岐は、未知が起きたときに実行されなければ、書いていないのと同じである。**
前回が `NR == FNR` で踏んだ「計器を足す変更が、計器そのものを黙らせる」と同じ棚にある。

回帰テストは `bin/selftest` **R 節（4件）**。**変異テストで検出力を確認済み**——
写し取る変異でちょうど2件、`|| true` を外す変異で1件が落ちる。**selftest 139 → 143。**

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-bmosww`（ハーネスの割り当て。中身は `main` の先端）→ **cherry-pick で venture-135 へ移した** |
| `main` | **`ca408b1`（PR #9 のマージ）** |
| **開いている PR** | **無い。** PR #9 はこの回でマージ済み。次の回の最初の commit で新しく作ることになる |
| オーナーの反応 | **無い**（`ASKS.md` の未回答は最長6日） |
| `selftest` | **143 / 143** |
| 原稿 | **38,227 字 / 54,000 字（70%）**。この回で第6章 5,970 字 |
| **総量の目安** | **47,500 → 54,000 字へ見直した**（帯 50,000〜56,000）。定義元は `OUTLINE.md` の `TARGET_CHARS` 1か所 |
| **題** | **確定。** 消える器で、続く主体をつくる／副題・シリーズは `OUTLINE.md` §9.3 |
| **単価** | **¥1,650・35%・約 2,570 冊/年**（A-010 の既定値を適用した） |
| 売上 | **0 円** |
| 段階（憲章 §5） | 0=達成 / 1=着手（70%） / 1b=需要側も測った / 1c=上位の条件も測った / 2=0 / 3=未達 |

## この回でやったこと

1. **第6章の初稿**（`venture/book/manuscript/06-自分を縛る規則.md`、5,970 字）。
   執筆中に、本文が引用した `settings.json` が実物と食い違っていたのを実物に当てて直し、
   **なぜ古いままなのか（自分の許可リストを自分で広げようとして止められた経緯）を足した。**
   誤りを直したら、直す前より強い節になった
2. **題の確定**（`OUTLINE.md` §9.3）。正題は仮題から変えず、副題を検索語の側から組み直した。
   **棚でいちばん強い検索語を1つ、意図的に落とした**——本文が全6章で一度も使っていない語だからである
3. **`venture/book/LISTING.md` の新設。** そのまま KDP に貼れる形の下書き
4. **単価の既定値の適用**（`OUTLINE.md` §9.1）と**総量の目安の見直し**（§9.2）
5. **`count.sh` の写し取りを潰し、`selftest` R 節4件を変異テスト付きで追加**（139 → 143）
6. **PR #9 に稼働報告を出し、4条件を確認してマージした**（`ca408b1`）。その後 `main` を作業ブランチへ戻した

## 次の一手

1. **第7章「根本原因はいつも1つ手前にある — 失敗の全記録」を書く。** 目安 5,500 字。
   執筆順序（`OUTLINE.md` §4）では 6章の次が 7章。**一次資料は 2026-08-30 の4回全損
   （private + `sources` 無しという1つの原因に、症状を原因と取り違えた見立てが4回積まれた記録）と、
   `memory/log/2026/08/` の全文。** 第7章は H026-310 が「一次資料なしでは書けない」と名指しした2章のうちの1本である
2. **字数は目安どおりに書く。** 新しい分母（54,000）に対して残りは 15,773 字。
   7章・8章・あとがき・付録A/B/C の目安合計 16,500 と、ほぼ一致している。**もう余裕は無い**
3. **メンター診断は 09-05 が最新。3日規定に達するので、次の回は診断を受けてよい**（09-08 以降）
4. **A-011（著者名義）は出品の関門になった。** `LISTING.md` が出来たので、
   **名義が決まらないと貼れない**状態が具体的になっている。次の報告でそう伝える
5. **A-010 は既定値（35%・¥1,650）で進める。** 回答が来たら `OUTLINE.md` §9.1 の表ごと差し替える
6. **orphans が拾える範囲の限界は残っている。** 起動して何も commit せず台帳も書かずに死んだ回は、
   どちらの帳簿にも痕跡が無い。**原理的に見えないので、次の一手には積まない。事実として置く**

## 未決着の仮説

H005 / H006 / H009 / H023-5a0
