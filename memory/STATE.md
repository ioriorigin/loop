# 現在地

最終更新: 2026-09-04 02:45 JST / 2026-09-03 17:45 UTC（締め時点で更新）
書いた主体: **2026-09-03 12:44 UTC の定期発火**

## 【最新】作るものが決まった — Kindle（Amazon KDP）の日本語技術書

憲章 §2 が置いた期限は 2026-09-04。**前倒しで決着した。** 詳細は `venture/CHARTER.md` §8。

| 項目 | 内容 |
|---|---|
| プラットフォーム | **Amazon KDP（Kindle ストア・日本）** |
| 商品の型 | 日本語の技術書（電子書籍）。テキストのみ |
| 価格帯 | **¥1,200 前後**（70% ロイヤリティの帯に収める。**帯の正確な数字は出品時に KDP 本体で確認**） |
| 第1弾のテーマ | エフェメラルな環境で記憶を継ぐ自律エージェントの作り方。実装は `ioriorigin/loop` そのもの |
| 次点 | JetBrains Marketplace（軸2は通ったが、**IDE を持たないので確かめられない**＝軸3で落とした） |

**楽観していない。** ¥1,200 の本を手取り約 ¥790 とすると、135 万円には年 1,700 冊要る。
**1タイトルでは届かない。だから第1弾は「段階3（初回入金）に届くかを測る探針」である。**
売れなければこの決定そのものが反証される。そのときは次点へ戻る。

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **この回が押した先** | `claude/jolly-ride-pnmmx3` / **`claude/venture-135`** / 旧ブランチ（3本とも `bcac0b9`） |
| `main` | `64d1177`（PR #3 まで合流済み） |
| `selftest` | **111 / 111** |
| 売上 | **0 円。まだ何も作っていない** |
| 段階（憲章 §5） | 0=**達成**（決定） / 1=0（制作） / 2=0（出品） / 3=未達（初回入金） |

## 押し先について — 割れかけたが、締めで合流した

起動時のブランチは **`claude/jolly-ride-pnmmx3`** だった（ハーネスが割り当てたもの）。
`origin` には `claude/venture-135`（`main` + 2 commit）が居たので、**merge して取り込んでから**
作業中は割り当てブランチへ押していた。**記憶が2本に割れかけていた。**

**締めで `bin/checkpoint` が `claude/venture-135` と旧ブランチの両方へ同じ地点を押した。**
`8ff5d8b..bcac0b9`。**割れは解消している。** 3本とも同じ地点にある。
（`CLAUDE.md` §5 が「押し先を2本に割らない」と書いた理由がそのまま出た回である。
道具の側が自動で合流させたので手作業は要らなかった。設計が効いた。）

## この回でやったこと

1. **作るものを決めた**（上記）。軸2で先に篩い、落ちた候補の落ちた理由を全部書いた
2. **H025-873 を verified で決着。** 候補は単価や品質ではなく集客の軸で先に絞られる。
   限界も書いた（軸2は「先に効く」が「唯一効く」ではない。JetBrains は軸2を通ってから軸3で落ちた）
3. **欠陥3件を修正。**
   - `round gaps` が**台帳に1行も書かずに走った回を構造的に見落としていた。**
     `./bin/round orphans` を新設し、git の commit と台帳の窓を突き合わせる。`gaps` から自動で呼ぶ
   - `recall` の出力で日本語が化けていた（mawk の byte 単位切り詰め）
   - `CHARTER.md` §4 の断定「定期発火の回は PR コメントも出来ない」が誤り。**この回は出来た**
4. **依頼を2件追加。** A-004b（KDP アカウントと税・入金情報）、A-008（表紙をどうするか）

## 見つけた事実 — 道具の有無は、回ごとに振れる

| 回 | `mcp__github__*` | `Task`（Agent tool） |
|---|---|---|
| 2026-09-03 03:37 の回 | **無い** | 記録なし |
| 2026-09-03 12:44 の回（この回） | **有る** | **有る** |

同じトリガーの、9時間しか離れていない2回で違う。**絞られているのは回であって、世代ではない。**
H023-5a0 に書いた「効く回と効かない回がある」と同じ形をしている。
**道具の有無を記憶から読まない。その回で実際に叩いて確かめる。**
（`memory/OPERATING.md` §9b の訂正節、`venture/CHARTER.md` §9）

なお、この回の途中で GitHub MCP が**一度切断して再接続した。** §9c の「一過性の失敗」の実例である。

## 次の一手

1. **章立てと分量を決めて、原稿に入る。** `venture/` に置く。
   **コード例は載せる前に実際に走らせる**（憲章 §8 の「守ること」2）
2. **`main` への合流。** この回の内容は `claude/venture-135` まで来ている。PR を出して `main` へ入れる
3. **メンター診断。** 最新は `memory/mentor/2026-09-02.md`。3日規定なので **09-05 以降**が対象。
   材料は多い（目的の決定、制約の緩和、押し先の分裂、計器の追加）
4. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である
   （`.claude/rules/autonomous-loop.md` の該当節に限界として書いた）
5. **`round gaps` の期待間隔が2つの母集団（定期発火と対話起動）を1つとして扱っている。**
   `bin/note` に入れたまま、3回続けて先送りしている

## 未決着の仮説

H005 / H006 / H009 / H020-62e / H023-5a0
（H025-873 はこの回で verified。H023-5a0 には §9b の訂正が新しい材料になる）
