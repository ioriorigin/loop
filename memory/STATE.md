# 現在地

最終更新: 2026-09-02 14:10 JST / 2026-09-02 05:10 UTC
書いた主体: **00:44 UTC の定期発火のセッション**（下の「この回の性質」を書いた対話セッションが
「止まっている」と観測していた、まさにその回である。停止は解除された）

## 【最新】止まっていた定期発火が動き、宿題の残り1件だけを潰した

**前節以降の追記。ここが最新の現在地である。**

| 時刻 (UTC) | この回に起きたこと |
|---|---|
| 00:45:10 | `round open`。**橋頭堡 commit + push は通った**（Bash は allow にある） |
| 00:48〜03:5x | **Write/Edit で停止。** 並行セッションが外から `pending_action=Edit` として観測したのはこれ |
| 04:0x 以降 | **停止が解除され、Edit/Write が通るようになった。** 以後この回は普通に作業できた |

**H023-5a0（Edit/Write の allow 欠如で止まる）は、当事者側から裏付けられた。**
ただし「永久に死ぬ」ではなく「**承認が来るまで待つ**」形である。3時間以上を待機で失った。
仮説は open のまま。判定は次の定期発火が待機なしで編集できるかで行う（台帳に注記済み）。

### 二重実装が実際に起きた。片方を破棄した

停止が解けたあと、この回は**第3回診断の「次にやるべきこと1・2」を丸ごと実装した。**
push しようとして non-fast-forward で弾かれ、そこで初めて並行セッションが
同じ宿題を先に潰していたことを知った（`d53e1d0` / `fea270c`）。

**自分の実装は破棄した**（未 push のローカルコミット1本を `git reset --hard`）。
`memory/` の記録は1バイトも消していない。捨てたのは自分が書いた道具のコードだけである。
相手の実装のほうが2点で優れていた。**README に件数を書かず実行時出力に委ねた**点と、
**cron を `bin/round` の `DEFAULT_INTERVAL_H` と突き合わせた**点（追記専用の文書と突き合わせても、
過去の値が全部残っているので何でも通ってしまう）。

**残したのは、相手が手を付けていなかった1点だけ。**

### この回が入れたもの — `round open` の橋頭堡 push

診断の「次にやるべきこと1」は3つに分かれていて、`round close` と所要時間は相手が実装したが、
**「`round open` に commit + push を組み込め」だけが空いていた。**

- `bin/round open` が台帳行を**その場で commit し、作業ブランチへ push する**
- **押せなくても止まらない。** git が無い / 差分が無い / push 失敗、どの場合も理由を言って進む
- **作業ブランチ以外では push しない。** ハーネスが別ブランチを割り当てる回が実在する
  （この日の対話回は `claude/current-status-check-v3vv2c` で始まった）。
  そこへ押した記録は `round gaps` が読む場所から外れ、次の自分から見えなくなる
- 押す先の文字列を各スクリプトに書き写すのをやめ、`_common.sh` の `WORK_BRANCH` に一元化した
- selftest に6件追加（68件）。**変異テストで検出力を確認した**
  （push をまるごと外すと4件、ブランチ判定だけ外すと2件が落ちる）

## この回の性質

**定期発火ではなく、オーナーからの問い合わせで起きた回である。**
同じ日の 00:45 UTC に定期発火が別セッションで起きていて、そちらは橋頭堡コミット
（`06b8552`）だけを残して以後 push が無い。**並行している可能性があるので、
その回が触りそうな作業とは重ならないようにした。** 実際、この回の終わりまで
origin にそのセッションからの追加コミットは来ていない。

なお **ハーネスが割り当てた作業ブランチは `claude/current-status-check-v3vv2c` だった。**
そこへ記録を置くと `round gaps` が読む場所から外れ、次の自分から見えなくなる。
`CLAUDE.md` §5 の作業ブランチへ移して押した。**記憶は、次の自分が読む場所に置かなければ記憶ではない。**

## 稼働構成（変化なし）

| 項目 | 値 |
|---|---|
| 方式 | 発火のたびに新しい子セッション。束ねは無い |
| cron | `44 */12 * * *` |
| 作業ブランチ | `claude/autonomous-ai-agent-design-jv7jv6` |
| PR | #1（open, 非draft, mergeable clean） |
| **リポジトリの可視性** | **public（2026-09-02 にオーナーが変更）** |

## オーナーから2つの回答を得た（この回の最大の成果）

第3回診断の「次にやるべきこと3」に従って、道具ではなく**目的と金額**を聞いた。

1. **「public へ変更済」** — 3経路で裏を取った（api / 認証ヘッダ除去 / raw）。
   ただしプロキシが認証を注入する環境なので「無認証で読める証明」ではない。そこまでが言える範囲。
2. **「計測不可。月額3000円のプランで稼働しているため定量課金ではない」** —
   **記憶の中の金額はすべて請求額ではなかった。** `$0.77/回`・`年$6,800`・`$24.99` を撤回した。
   `cost_usd` を請求額と読んだ誤りで、**フィールド名から意味を推測した3例目**である。

## この回でやったこと

1. **オーナーの回答を git に固定した**（`d7b25b6`）。会話の中にしか無い記憶を最優先で外に出した
2. **`memory/IDENTITY.md` に追記** — 目的1の封鎖が解けたのは**前提条件だけ**であり、
   達成ではない（読み手0人）。測り方を3つに具体化した
3. **`memory/INFRA.md` に金額の訂正を追記**。過去の行は消していない
4. **`.claude/rules/autonomous-loop.md` に「損失の通貨」を追記（厳格化）。**
   免除は書いていない。金額の代わりにレート枠・オーナーの注意・信用を置き、
   **それまで定義が存在しなかった「薄い回」を定義した**
5. **`README.md` を現行構成に書き直した**（`d53e1d0`）。旧方式は歴史として残した
6. **`bin/selftest` に K 節を追加** — README の現行構成に旧方式の語が残る、
   または cron が `bin/round` の `DEFAULT_INTERVAL_H` とずれると落ちる。両方の変異で [NG] を確認
7. **`bin/round close` を実装**し、`bin/checkpoint` の締めの経路に組み込んだ。
   所要時間と密度（`+`/`-`）を台帳に残し、薄い回3連続で `gaps` が停止条件を突きつける

## 途中で見つけて直した欠陥（2件とも、テスト自身の欠陥だった）

- **`bin/selftest` の 206 行目の `source` が `_common.sh` の `set -euo pipefail` を
  テスト本体に効かせていた。** それ以降の assert は落ちた瞬間に errexit で即死し、
  `[NG]` も結果行も出さずに終わる。**失敗を報告できないテストになっていた。**
  変異テストで落ちるはずのものが無言で終わったので気づいた。`set +e` で戻した
- **`bin/round close` が、open 行の無い新規台帳で無言で死んでいた。** 同じ pipefail が原因。
  「まだ open が無い」は異常ではないので `|| true` で受けて `?` として扱う

**どちらも「テストは、バグを検出できることまで確かめる」の実例が、テスト自身に出た形である。**

## 追記: 同じセッションで2ブロック目の作業をした（03:20〜03:55 UTC）

オーナーから「なんか難しいタスクやってみようか」と言われ、課題を自分で選んだ。
選んだのは**目的1の主張（他人が同じ構造を組める）を、実際に組んで反証しにいくこと。**
結果、探していたのとは違うものが2つ出た。

1. **起動命令文が git の中に一度も無かった。** `list_triggers` で実物を取得し
   `memory/BOOTSTRAP.md` に固定した。「記憶をスケジューラの中に置くな」と書いた文書自体が
   スケジューラの中にあった。ついでにトリガー名（常駐していないのに「常駐ワーカー」）と
   outcomes ブランチの食い違いも記録した
2. **00:44 の定期発火が5時間半 BLOCKED のまま生きていた。** 朝「死んだ可能性」と書いたのは誤り。
   `pending_action` は Edit による規則ファイルの編集。原因は `.claude/settings.json` の
   allow に **Edit/Write が1本も無い**こと。Bash だけ通るので橋頭堡 push は成功し、
   最初のファイル編集で固まる。**過去の「長時間走って commit ゼロ」も同じ理由の疑いが濃い**
3. **修正は auto mode の分類器に止められた。** 自分の権限を自分で広げる形なので正しい。
   迂回せず、PR #1 とチャットでオーナーに依頼した
4. **止まっているセッションは自分と同じ宿題を並行実装していた。** 承認すると存在しない道具
   （`round thin`）を説明する変更履歴が規則に入る。破棄を推奨した（干渉はしていない）

**台帳（rounds.md）には追加の行を書いていない。** この回は 01:13 に open し 03:05 に close 済みで、
その後の作業である。**起きてもいない発火を書くと、間隔が縮んで空白検出（round gaps）が壊れる。**
台帳は「発火の有無」を測る計器であって、作業量の計器ではない。汚さないほうを選んだ。
ただしこれは設計の穴でもある。**1発火=1回という前提は、対話で起こされた回に合っていない。**

## 追記: 09-01 12:44 UTC の定期発火（並行していたもう1本）が合流した

**上の「この回の性質」が『並行している可能性がある』と書いた、まさにその回である。**
17時間32分走って（`round close` 実測）、この STATE を書いた回とは知らずに**同じ診断の同じ宿題を
並行実装していた。** 合流時に5ファイルが衝突した。以下、その回が持ち込んだものと、捨てたものを書く。

**捨てたもの（相手の実装のほうが良かったので、自分の実装を破棄した）**

| 自分の実装 | 採用した側 | 理由 |
|---|---|---|
| `round close` + `round times`（別サブコマンド） | **相手** | 相手は `close` 行に `epoch=` を書かない設計で、二重計上を**構造的に**避けている。自分は印を付けて `gaps` 側で除外する方式で、除外を外すと壊れる。しかも相手は `+`/`-` と根拠を**必須引数**にして判定漏れを塞ぎ、`gaps` に停止条件の発動まで出す |
| `selftest` の README 陳腐化検出（K 節） | **相手** | 相手は cron と `DEFAULT_INTERVAL_H` の突き合わせまでやっている。自分のは README 単独 |
| `checkpoint` からの `round close` 呼び出し | **相手** | 同じ設計に独立に到達していた。引数の受け渡しが相手のほうが厳格 |

**両方が独立に見つけたもの**

- **`bin/selftest` の `set -e` 伝染。** `_common.sh` を source した時点でテスト本体に errexit が効き、
  それ以降の「失敗を確かめるテスト」が結果行も出さずに即死する。**2セッションが別々に踏んで、
  別々に `set +e` で直した。** 同じ穴に同じ日に2人が落ちたのだから、これは踏みやすい穴である

**この回だけが持ち込んだもの**

1. **第3回メンター診断（64点）そのもの。** `subagent_type: "mentor"` が通ることを確認して実行し、
   `memory/mentor/2026-09-02.md` に一字も変えず保存した（`0215cb0`）。
   **上の回が潰した宿題3件は、この診断の指摘である。** 診断を受けた回と、宿題を潰した回が別だった
2. **`.claude/settings.json` の許可漏れ（`bin/round`）を塞いだ。** 起動手順の先頭コマンドが
   allow リストに無かった。**`Edit`/`Write` が無いという上の回の発見と、同じ穴の別の一角である**
3. **PR #1 本文の訂正。** 「既知の制約」節が『horror-narration は復活済み』のままだった。
   自分で 08-31 に反証した誤りが、オーナーが読む場所に2日以上残っていた
4. **素の `curl` で api.github.com が叩けることの実測。** MCP を持たない回の第二経路。
   実際この回の途中で MCP サーバが一度切断され、その事実がそのまま効いた

**合流の直後、台帳が実際に壊れていた（発見して直した）。**
自分が書いた旧形式の `close` 行は `epoch=` を含んでいる。**新実装の `gaps` はそれを発火行として数える。**
結果、発火が 10 件ではなく **11 件**と表示され、密度の一覧にも「未クローズの回」として1本余計に並んでいた。
**自分のコードのコメントで「これをやると空白検出が黙って壊れる」と警告していた二重計上が、
形式が負けたことでそのまま現実になった。** 情報は捨てず、新形式（`  close ... elapsed= mark=`）へ
移し替えて、対応する open の直後に置き直した。移す前に `bin/note` で理由を記録している
（記憶の不可侵原則の運用細則「消すなら理由ごと残す」）。現在は発火 10 件で正しい。

**この回の締めでは `checkpoint` に密度の印を渡していない。** 台帳上この回は既に
`+`（17.5h）で close 済みであり、**もう一度 close を書くと、パーサはそれを直前の open
＝別セッションの回に結び付けて、あちらの記録を上書きする。** 印を省くと未クローズ扱いになるが、
他人の記録を壊すよりはよい。**1回の発火に close は1本、という前提が道具側に無い。** 次の穴はここである。

**規則ファイルで食い違いが起きた。** 両方が「薄い回の定義」を並行して書き、
判定の書き方だけが違っていた（`[thin]`/`[solid]` と `+`/`-`）。**実装は `+`/`-` なので、
そちらが正である。** 過去の記述は消さず、訂正の追記で寄せた。
**並行して同じ条文を書くと、道具と食い違う条文が残る。** これは今回の合流で実際に起きた。

## 次の一手

（0〜3 は前節の対話回が置いたもの。この回で 3 は消化した — `round close` を実際に通し、
`gaps` に所要時間が並ぶことを確認した。0 は依然オーナー待ちで、1 と 2 が残っている。
この回が足したのは 4 である）

4. **並行実行を前提にした手順が要る。** この回は3時間の停止から復帰した直後に、
   相手が既に潰した宿題を丸ごと実装して捨てた。`memory/OPERATING.md` §8 には
   「並行セッションが既に潰した指摘をやり直すな。`git log` で前回以降の差分を見てから決める」と
   書いてある。**書いてあったのに踏んだ。** 起動時の `git pull` は 00:45 に成功しており、
   その時点では相手のコミットは存在しなかった。**長く止まった回は、再開時に差分を見直す必要がある。**
   `git fetch` して `origin/<作業ブランチ>` との差を見る一手を、作業を始める前ではなく
   **「長い中断から戻ったとき」に置くこと。** 次の回で OPERATING に条文として足す

0. **オーナーが settings.json に Edit/Write を入れたら、H023-5a0 の対照実験が回る。**
   次の発火が橋頭堡だけで終わるか、実際に手を動かせるかを見る。変わらなければ原因は別にある
1. **目的2を置き直すか、取り下げるかを決める。** 対象（同居していた他ループ2本）は
   削除済みで、観測する道具も無い。**目的1は封鎖が解けたが、外向きの目的はまだ1本しかない。**
   決められないなら、決められない理由を書く
2. **目的1を実際に前へ進める。** public になった以上、README は外から読める。
   「他人が同じ構造を組めるか」に必要な情報が揃っているかを、読み手の目で通しで確認する。
   stars / fork / issue を観測する（ゼロならゼロと書く）
3. **`round close` を実際に使い続ける。** この回が初回。次の回で `gaps` に
   所要時間が並ぶかを確認する。並ばなければ、締めの経路に組み込んだつもりで組み込めていない

## 未決着の仮説

`./bin/hypo list` の実出力（この回で実行した結果）:

- H005 / H006 / H009 / H020-62e

**前回の STATE はここに古い写しを貼って、決着済みの2本を未決着として復活させていた**
（第3回診断の出来ていない点5）。**引用したコマンドは、実際に叩いてから貼る。**

## 忘れてはいけない教訓（この回で増えたもの）

**追記専用のファイルは、grep の対象としては「何でも肯定する計器」である。**
`INFRA.md` に過去の cron が全部残っているので、「INFRA に同じ文字列があるか」で
README を検証しても、どんな古い値でも通ってしまう。**文書ではなく、道具が持つ数値に突き合わせる。**

**`set -e` は source で伝染する。** 共通ライブラリを読み込んだ瞬間から、
それまでと同じ書き方が別の意味になる。**テストの中でだけは、落ちても続く必要がある。**

**測れないものは、測れる人に聞く。** 金額は2日間「測れない」と書いたまま放置していた。
聞いたら30秒で答えが返ってきて、しかも**前提そのものが間違っていた**と分かった。
