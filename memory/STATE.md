# 現在地

最終更新: 2026-09-05 22:10 JST / 2026-09-05 13:10 UTC（締め時点で更新）
書いた主体: **2026-09-05 12:44 UTC の定期発火**

## 【最新】需要側を初めて測った。棚の中央値では目標に届かないことが確定した

**この回のいちばん重い発見は、数字そのものより「前回の結論が誤りだった」ことのほうにある。**

> 前回は「Amazon が `WebFetch` に HTTP 500 を返すので需要側は測れなかった」と書いた。
> **同じ URL を `curl` にブラウザの UA を付けて叩いたら 200 で返る。**
> 試したのは2つの URL であって、**2つの道具ではなかった。**

実測（`venture/MARKET.md` §7、類書15冊、母集団は KU 登録・叢書名なしの日本語 Kindle 本）。

| 量 | 値 | 憲章が置いていた前提 |
|---|---|---|
| レビュー数 **中央値** | **0 件**（15冊中10冊が0件） | 1冊あたり年 340 冊 |
| 価格 中央値 | **¥600** | ¥1,200 |
| ページ数 中央値 | 169 ページ | （本書は 47,500 字 ≒ 130〜150 頁） |

**H028-53f を verified で決着**（判定基準は調べる前に台帳へ書いた）。
**棚の中央値の本を書いても 135 万円には届かない。** これは「やめろ」ではなく、条件の確定である。

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-31uwnn`（ハーネスの割り当て）→ **merge で venture-135 へ移した** |
| `main` | `0703cf0`（PR #6 まで合流済み） |
| **開いている PR** | **#7（09-05 01:21 作成）。マージしていない。この回の作業も同じ PR に載る** |
| `selftest` | **125 / 125**（116 から O 節 9 件を追加） |
| 原稿 | **20,246 字 / 47,500 字（42%）。この回は1字も書いていない**（診断の指示どおり） |
| 売上 | **0 円** |
| 段階（憲章 §5） | 0=達成 / 1=着手（42%・**停止中**） / **1b=需要側も測った**（制度＝確定・需要＝中央値を実測） / 2=0 / 3=未達 |

## この回でやったこと

1. **需要側の一次データを初めて取った。** 類書15冊（価格・レビュー数・ページ数・KU・刊行日）。
   取得と抽出のスクリプトを `venture/market/` に置いた。**次の回が測り直せる**
2. **H028-53f を verified。** 判定基準は調べる前に書いた。限定（換算率に出典が無い／KENP が見えない／
   刊行が新しい／無作為抽出でない）を同じ節に併記した
3. **前回の「測れなかった」を訂正し、手順化した。** `OPERATING.md` §7b。
   **外部の読み取りが失敗したら、結論を書く前に別系統の道具を1つ試す**
4. **台帳の計器が同時に2つの嘘をついていたのを直した。** `gaps` の「24時間の空白」と
   「締めを飛ばした回1件」は**両方とも捏造**だった。原因は**台帳の行順が時刻順でないこと**
   （merge=union で新しい open が古い open の前に入る）。`orphans` も窓が裏返り、
   **この回自身の commit を告発していた**
5. **「いま走っている回」を位置ではなく `.round-current` の印で外すようにした。**
   印は既に存在していた。**書く側（`for=`）は 09-02 に直したのに、読む側は位置のままだった**
6. **`selftest` O 節 9 件を追加。変異テストで検出力を確認**（3種の変異でそれぞれ 1 / 2 / 1 件が落ちる）
7. **A-010 に需要側の数字を追記。** ¥600 / 35% なら 1冊 ¥210 で、必要冊数は年 **6,430 冊**。
   A-010 の未回答が固定すると難易度は倍ではなく **3.6 倍**になる

## 次の一手

1. **【最優先】この棚の中央値ではなく上位に入る根拠を、H026-310（一次資料の密度）以外に持っているか。**
   持っていないなら、目標に届く形は「1冊を上位に押し込む」ではなく別の形である。
   **`CHARTER.md` §8「確かめられないものを売り物と呼ばない」を、売り方にも当てる。**
   外れ値2冊が示した道（初心者向け・低価格・多レビュー）は本書の設計と逆を向いている。
   **この問いを H029 として登録し、判定基準を先に書いてから調べる**
2. **A-010 の回答が来たら原稿を再開する。** 来ないまま執筆回が来たら
   **35%・¥1,650 で組み直して書き続ける**（既定値は `MARKET.md` §3.3）。**止まって待たない**
3. **PR #7 をどうするか。** この回は開けたまま締め、**PR コメントで報告した**（本文ではなく）。
   次の回は、オーナーの反応が無ければマージを判断してよい。**判断したことをログに書く**
4. **`round gaps` の期待間隔が2つの母集団（定期発火と対話起動）を1つとして扱っている。**
   **7回続けて先送りしている。** 先送りの回数のほうを先に数え始めるべきかもしれない
5. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である（この回で偽陽性は消えた）
6. **メンター診断は 0 日前（09-05）。** 次は 09-08 以降

## 未決着の仮説

H005 / H006 / H009 / H023-5a0
（**H028-53f はこの回で verified**）
