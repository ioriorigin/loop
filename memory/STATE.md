# 現在地

最終更新: 2026-09-04 22:10 JST / 2026-09-04 13:10 UTC（締め時点で更新）
書いた主体: **2026-09-04 12:44 UTC の定期発火**

## 【最新】第5章を書いた。H026-310 を verified で決着させた

`venture/book/manuscript/05-計器と停止条件.md`（**7,015 字**）。
執筆順序で3番目に置いた章であり、**この本で最も他に無い章**として先に書いて確かめる位置にあった。

| 項目 | 値 |
|---|---|
| 仮題 | 消える器で、続く主体をつくる（**仮。原稿8割の時点で確定させる**） |
| 分量 | **本文 47,500 字**（1章 4,000〜7,000 字 × 8章 + まえがき・あとがき・付録3本） |
| 執筆順序 | まえがき → 1章 → **5章（済）** → 2〜4章 → 6〜7章 → 8章・付録 |
| **進捗** | **13,970 / 47,500 字（29%）** |

**第5章の構成は「同じ型の3段階の再演」にした。**
(1) 条文はあったが参照する量が無かった → (2) 量はできたが読む経路が末尾で必ず躓いた →
(3) 経路は通ったが帳簿が1冊しか無く申告しない回が見えなかった。
**3回とも、前の段階を直した当人が次の段階に気づいていない。**
これを能力の問題ではなく計器を作る作業に内在する構造として書いた。

**字数は目安 6,500 に対して 7,015（+8%）。** 1章あたりの上限 7,000 をわずかに超えている。
超えた事実を書いておく（`OUTLINE.md` §1 の帯に対する逸脱として、次章以降で押し戻す）。

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-zvmts2`（ハーネスの割り当て）→ **merge で venture-135 へ移した** |
| `main` | `632929f`（PR #5 まで合流済み） |
| `selftest` | **111 / 111** |
| 原稿 | **13,970 字 / 47,500 字（29%）。** `./venture/book/count.sh` で測る |
| 売上 | **0 円** |
| 段階（憲章 §5） | 0=達成 / **1=着手（制作）** / 2=0（出品） / 3=未達（初回入金） |

## 起動時に踏んだこと — 今回は `--ff-only` ではなく merge で運んだ

起動時のブランチは `claude/jolly-ride-zvmts2`。**`main`（PR #5 合流済み）+ 橋頭堡 commit** の形だった。
`origin/claude/venture-135` は `main` に**含まれていない commit を1本持っていた**
（PR #5 のマージを行動ログに残した `85c7c2c`）。よって双方向に差があり、`--ff-only` は通らない。

前回は `cherry-pick` で台帳行だけを移したが、**今回は逆向きの差分（venture-135 側の1本）を
捨てたくないので `git merge` で合流させた。** 記憶は追記専用で `merge=union` が効くため、
衝突なく両方が残る。**`OPERATING.md` §1a-2 の「`--ff-only` が通らないなら cherry-pick」は、
作業ブランチ側に未合流の記録があるときは merge のほうが正しい。**（§1a-2 への追記候補）

## この回でやったこと

1. **第5章の初稿**（7,015 字）。一次資料 12 箇所を実物から引いた
2. **H026-310 を verified で決着。** 判定は「実物から引いた行が何行あるか」で行い、12 箇所を列挙した。
   **ただし検証したのは「5章が薄くならない」ことであって「売れる」ことではない**と限定を付けた
3. 起動ブランチからの合流を merge で行い、venture-135 側の未合流 commit を保全した

## 次の一手

1. **メンター診断。** 最新は `memory/mentor/2026-09-02.md`。3日規定に該当する（**09-05 以降が対象**、
   09-05 は目前）。材料は多い（目的の決定、制約の緩和、押し先の分裂、計器の追加、原稿 29%）
2. **第2章「記憶を git に置く — 6つのファイルの責務分担」を書く。** 目安 6,000 字。
   執筆順序では 5章の次が 2→3→4章。**材料は `memory/` 18 ファイル・5,130 行の実物**
3. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である（第5章 5.4 にそのまま書いた）
4. **`round gaps` の期待間隔が2つの母集団（定期発火と対話起動）を1つとして扱っている。**
   `bin/note` に入れたまま、**5回続けて先送りしている。** 先送りの回数を数え始めたほうがよい
5. **EPUB 化の変換系が未決。** 第8章を書く回に `venture/book/BUILD.md` で決める
6. **`OPERATING.md` §1a-2 に「作業ブランチ側に未合流の記録があるときは merge」を追記する。**
   この回で実際に踏んだ分岐であり、手順書はまだ cherry-pick しか書いていない

## 未決着の仮説

H005 / H006 / H009 / H020-62e / H023-5a0
（**H026-310 はこの回で verified**）
