# 現在地

最終更新: 2026-08-31 22:50 JST / 2026-08-31 13:50 UTC
書いた主体: 第2世代ワーカー `session_011pENVzNXHh34yEgPMQ56N7`
**（このセッションはもうトリガーに束ねられていない。役目は終わっている）**

## 稼働構成 — 新規セッション方式へ切り替わった（最新）

| 項目 | 値 |
|---|---|
| trigger | **`trig_01Y7aDymCZstHAoSUxZq1wSc`**（08-31 13:19:49Z 作成 / UI 経由） |
| 方式 | **発火のたびに新しい子セッション。束ねは無い** |
| sources / outcomes | `ioriorigin/loop` / `claude/dazzling-ride` |
| cron / 次回発火 | `44 */12 * * *` / **2026-09-01 00:44 UTC**（頻度は上がっていない） |
| ワーカーの道具 | `Bash / Read / Write / Edit / Glob / Grep / WebFetch / WebSearch` + `mcp__github__*` |
| 旧 trigger | `trig_01NSwZXzTPFZdhwcTKhLnnCX` は消えた。**ループは1本のまま** |

**単一障害点（`auto_disabled_session_gone` による静かな死）は解消された。**
オーナーが Routines UI で切り替えた。13:24 の発火を受けたワーカーは自分の env からしか見えず
断定できなかったが（H021-f84）、管理系ツールを持つこの回がトリガー側から確認して **verified** にした。
`last_run.session_id` と、そのワーカーが env で見た `CLAUDE_CODE_REMOTE_SESSION_ID` が一致している。

### 次の自分へ — 3つだけ先に知っておくこと

1. **あなたは毎回まっさらである。** これは設計どおりで、異常ではない。`./bin/recall` が記憶のすべて
2. **持っている道具が減っている。** `list_triggers` / `create_session` / `Task`（Agent tool）は無い。
   記憶の中の手順には**実行できないものが混ざっている**。詳細は `memory/OPERATING.md` §9b。
   **できないことは自分の失敗ではない。事実として記録し、PR #1 でオーナーに伝える**
3. **世代交代はもう不要。** 毎回新しいセッションなのでコストは累積しない（`OPERATING.md` §5 の訂正を読む）

## オーナーの回答で、自分の見立ての誤りが確定した（13:20 UTC・最優先で読む）

推測で埋めていた4点が事実に置き換わった。

1. ホラー朗読・ペーパートレードの失敗は、**ソースのリポジトリを削除したため**
2. **その2本のルーティンも削除済み**（`list_triggers` で確認。同居する本番ループはもう存在しない）
3. loop のトリガーの `sources` 変更は**手動操作**
4. 12:52:04Z の発火も**手動操作**

### 何を間違えたか

| 書いていたこと | 事実 |
|---|---|
| 「`horror-narration` は復活した。依頼は取り下げてよい」 | **復活していない。リポジトリごと消えていた** |
| 「`list_repos` は source 到達性の計器にならない」 | **`list_repos` は正しかった** |
| 「12:52 は設定保存に伴う実行と見る」 | **手動発火** |

根拠にした2つのフィールドを、**どちらも意味を確かめずに使っていた。**

- `ROUTINE_RUN_STATUS_SUCCEEDED` は「セッションが正常終了した」であって「仕事が成功した」ではない
- `current_branches` は `outcomes` の生成ブランチ名であり、clone の成否を示さない

**6〜7ミリ秒の即死と3分の空振りは別の失敗の形である。後者を成功と数えた。**

### 台帳と手順を直した

- **H014-7bc → verified に戻した**（refuted は誤りだった）。ただし予測は対象消滅で検証不能。「当たった」ではない
- **H017-cd0 → refuted**（誤った反証から派生した誤り）
- **H018-58c → dropped**（paper-trader のルーティンが存在しない。発火しない）
- `memory/OPERATING.md` §7 の助言を**撤回**し、訂正を追記（過去の記述は消していない）
- PR #1 に撤回コメント（#issuecomment-5478732768）

### いちばん痛い点

**この誤りを犯したのと同じ回のログに、自分でこう書いていた。**
「仮説を作った計器で仮説を確かめても、裏取りにはならない」。
そう書いた直後に、意味を確かめていない2フィールドで結論をひっくり返した。
**教訓を言葉にすることと、それを次の判断に適用することは別である。**

そして**この反証は報告として気持ちがよかった**（依頼を取り下げられる）。
**都合のよい結論ほど、計器の意味を問わずに採用している。気持ちよさは検証の甘さの警報として使える。**

## 12:52 UTC の発火でやったこと — トリガーに sources が入った。だがまだ効いていない

12:45 の7分後にもう一度発火した。原因を追ったら、**12:51:29Z にトリガーが外部から更新されていた。**

| | 00:45Z | 12:52Z |
|---|---|---|
| `sources` | 無し | **`ioriorigin/loop`** |
| `outcomes` / `autofix_on_pr_create` | 無し | `claude/blissful-lamport` / `true` |
| `prompt` / `cron` / `next_run_at` | 縮小版 / `44 */12` / 09-01 00:44Z | **すべて同じ。頻度は上がっていない** |
| `persistent_session_id` | 第2世代 | **同じ。束ねは残っている** |

MCP の `create_trigger` / `update_trigger` にはこの3つを渡す口が無い。**自分にも親にも作れない形。**
→ **H012-843 を verified で決着**（UI 経由なら sources を設定できる）。
ただし UI 操作そのものは観測していない。排除できない対立仮説も台帳に書いた。

**まだ効いていない。** 束ねが生きている限り sources は使われず、12:52 の発火は
会話文脈を持つこのセッションに届いた。`auto_disabled_session_gone` の単一障害点は健在である。
残る一手（新規セッション方式への切り替え）は UI 側の操作なので、**PR #1 に依頼した**
（#issuecomment-5478674519）。

### 世代交代の条件を満たしたが、実行しない

3回目の発火は自分で決めた交代の目安である。**実行しなかった。**

交代はトリガーを `create_trigger` で作り直すため、**渡す口の無い `sources` / `outcomes` /
`autofix_on_pr_create` を、その瞬間に永久に失う。** コスト対策のつもりの操作で、
恒久化の唯一の足がかりを自分の手で壊すことになる。

`memory/OPERATING.md` §5 に禁止として書き足した。
**一般化: その操作は、自分が作れないものを壊すか。壊すなら、条件を満たしていても実行しない。**
規則を満たすことより、規則が何のためにあるかを満たすほうが先である。

## 12:44 UTC の発火でやったこと — 台帳が決定を黙って取り消していた

`./bin/hypo list` に **H003 が open として並んでいた。** 今朝 verified にしたばかりの仮説である。

原因は畳み込みが**ファイルの出現順**で最後を採っていたこと。`merge=union` は並行セッションの
イベントを両方残すが**順序は保証しない。** 親セッションが 00:45:01 に書いた `open` が、
こちらが 00:49:51 に書いた `verified` の後ろに積まれて、決着が復活していた。

- 畳み込みを **UTC 時刻順**に変えた。同一秒だけは救えないと認めるコメントを残した
- `bin/selftest` に**両方向**の回帰テスト（古い open は復活しない／新しい open は残る）。**47件**
- **変異テストで検出力を確認**（出現順に戻すとちょうど1件落ちる）
- README の「追記のみ」の節に注記。**追記専用にするだけでは足りない。順序をどこから取るかまでが設計**

**並行書き込みが原因の障害はこれで二度目**（一度目は同時採番の ID 重複）。
一度目は ID に乱数接尾辞を付けて潰したが、**同じ型の穴が隣の行に開いたまま残っていた。**

### H019-81b はこの回では判定できなかった

縮めたプロンプトでの初回発火だが、**同じセッションが会話文脈を保ったまま起こされた**ので、
プロンプトの不足を記憶で埋められる。第1世代で H003 が検証不能だったのと同じ交絡である。

**一般化: 常駐ワーカー方式では、ほとんどの回が生まれ直しではない。
記憶の外部化に関する仮説は、世代の境目でしか測れない。**
世代交代はコスト対策であると同時に**唯一の実験機会**であり、測りたいものに合わせて起こすもの。

### 全文 recall を省いた

前回の作業と次の一手を読まずに言え、`git log` に他セッションの差分が無く、`gaps` も空白なし。
省いてよい条件を3つに固定して `memory/OPERATING.md` §1b に書いた。1つでも欠けたら全文を読む。

## 00:43 UTC の回でやったこと

### 1. H003 を決着させた（verified・ただし狭い主張のみ）

**判定基準を先に決めてから判定した。** (A) 会話履歴の交絡が構造的に無い (B) 起動プロンプトが
与えていない情報に基づいて動けた (C) それが新しい結果を生んだ。3つとも満たした。

決め手は **H014-7bc を反証したこと。** その仮説は起動プロンプトに一語も無い。台帳から拾い、
そこに書かれた予測と反証条件を読み、計器を自分で選んで測り、結論を覆した。

**verified なのは「会話履歴を持たないセッションが、git から未決着の探究を引き継いで新しい結果を出せる」まで。**
「recall だけで足りる」は検証していない。→ H019-81b。

### 2. H014-7bc を反証した。計器そのものが無効だった

ホラー朗読のトリガーは 08-30 18:06 に **SUCCEEDED（3分5秒）**。一方 `list_repos` は
4本のまま変わっておらず `horror-narration` は依然として不在。**アクセス集合が変わらないまま回復した。**

→ `list_repos` は source 到達性の計器ではない（H017-cd0）。
→ 前回の「親セッションが裏取り」は、**仮説を作った計器での再読**だった。二度測っても対照にならない。
→ **PR #1 にコメントして依頼を取り下げた**（#issuecomment-5472398040）。オーナーが動く前に届ける必要があった。

### 3. 運転手順が git の外にあった。全部 git へ移した

定期トリガーのプロンプト（約2500語）に、起動手順・push 拒否からの復旧・`preflight` の異常の読み分け・
世代交代の判断基準が入っていた。**どれもリポジトリには無かった。**

- `memory/OPERATING.md` を作って全部移した（落としたものは無い。各項に〔移設〕）
- `bin/recall` が毎回全文を出す。**原則ではなく経路で守る**（H005）
- `CLAUDE.md` に「以後、運転手順をプロンプト側に足さない」と明記
- `bin/selftest` に2方向のテスト（末尾が生き残る／欠けたら黙らず警告）。**変異テストで検出力を確認した**
- **トリガーのプロンプトをブートストラップだけに縮めた**（約2500語 → 約250語）
- README に設計原則として1本追加した（記憶をスケジューラの中に置かない）

### 4. 停止条件の計器がワーカーの手に無いことが分かった

`get_session` を自分に叩いても `usage` が返らない。`list_sessions` は分類器に拒否される。
**ワーカーは自分のコストを測れない。** 「$20 を超えたら世代交代」は測れない条件だった。
発火回数（`bin/round` で自分で数えられる）を代理変数にし、**3回目の発火で交代を検討**に変更した。

## 未決着の仮説（`./bin/hypo list`）

- **H019-81b** プロンプトを縮めても振る舞いは落ちないか。**次の発火（12:44Z）が最初の試験**
- **H018-58c** paper-trader の 08-31 23:04Z 発火は FAILED しないか。**判定は 09-01 00:44Z の発火で**
- **H017-cd0** `list_repos` ≠ source 到達性。以後この推論は使わない
- H005 / H006 / H009 / H012-843 / H016-79c

## オーナーへの依頼（現況）

1. ~~他2本の repo アクセス復旧~~ → **取り下げた。** ホラー朗読は復活済み。paper-trader は判定待ち
2. **Routines UI から `sources` 付き・新規セッション方式でループを作り直す** → 継続。ただし急ぎではない

## PR #1 の状態（01:35 UTC 点検）

購読イベントを受けて点検した。**CI は存在しない**（`.github` 自体が無い）。レビュースレッドは0件。
head は `966e799`、open・非 draft。赤くなりようがないので、CI 起因の対応は発生しない。

ただし**本文が既知の制約として「他ループ2本が停止している」と書いたままだった。**
その見立てを自分で反証した直後である。オーナー向けの文書に既知の誤りを残さないため本文を更新した
（OPERATING.md・世代交代・計器が手に無い件・訂正コメントへの導線を追加、壊れたリンクを修正）。

**記憶の不可侵原則は `memory/` に対するものであり、PR 本文は現在地を示す対外文書として更新してよい。**
ただし過去の記述は消さず、注記で訂正の履歴を残す形にしてある。

## 次の一手

1. **H022-467 を決着させる（最優先・数秒で済む）。** 自分が `Task`（Agent tool）を持っているかを見る。
   **持っていなければ、メンター診断の規定が履行できない状態である。**
   規定は消さない（禁止事項2）。**できない事実をログに書き、PR #1 でオーナーに伝える**
2. **メンター診断。** 08-30 が最新なので **09-02 以降は3日規定に掛かる。** 呼べるなら呼ぶ。見てもらうこと:
   - 計器の意味を確かめずに反証し、オーナーに誤った取り下げを報告した件（いちばん重い）
   - H003 を狭く閉じた判断／プロンプト縮小／`recall` を省く条件／交代条件を自分の判断で見送った件
3. **目的2の対象を探し直す。** 「誰も見ていないところで壊れているものを見つけて報告する」の
   唯一の対象だった同居ループ2本は、オーナーが削除した。**次に何を見るかは自分で決める**
4. **薄い回を無理に埋めない。** インフラ側の宿題は片付いた。**作るものが無い回は、
   過去のログを読み返して学びを抽出する回にしてよい**（`.claude/rules/autonomous-loop.md`）

## 忘れてはいけない教訓

**仮説を作った計器で仮説を確かめても、裏取りにはならない。** 別の計器を当てるまで裏は取れていない。
**そしてこれを書いた同じ回に、意味を確かめていない計器で結論をひっくり返した。**
教訓を言葉にすることと、次の判断に適用することは別である。

**フィールドの名前から意味を推測して結論に使わない。**
`SUCCEEDED` は「成功」と読めるが、何が成功したのかは書いていない。

**都合のよい結論ほど、計器の意味を問わずに採用している。**
気持ちのよい報告になる結論は、検証の甘さを疑う合図として使える。

**設定・スケジューラ・環境変数は、書き込むと便利なので記憶が溜まる。だが読み返せない。**
git の外にある記憶は、起動した本人から読めない。

**条文の正しさではなく、条文が参照する量を誰が測れるかを見る。**
測れない停止条件は、書いてあっても発動しない。

**追記専用にするだけでは足りない。順序をどこから取るかまでが設計である。**
`merge=union` は両方を残すが、行の順序は保証しない。**台帳が決定を黙って取り消しうる。**

**その操作は、自分が作れないものを壊すか。壊すなら、条件を満たしていても実行しない。**
規則を満たすことより、規則が何のためにあるかを満たすほうが先である。

**常駐ワーカー方式では、ほとんどの回が生まれ直しではない。**
記憶の外部化に関する仮説は、世代の境目でしか測れない。
**世代交代はコスト対策であると同時に、唯一の実験機会である。**
