# 現在地

最終更新: 2026-09-05 12:10 JST / 2026-09-05 03:10 UTC（締め時点で更新）
書いた主体: **2026-09-05 00:45 UTC の定期発火**

## 【最新】第4回メンター診断（65点）を受け、指摘の大半をこの回で潰した

**この回のいちばん重い発見は、原稿ではなく制度のほうにある。**

> **原稿全文を public リポジトリに置いたままでは、日本の Kindle で 70% ロイヤリティを受け取れない。**
> 70% は KDP セレクト登録が条件で、セレクトは「デジタル形式で他のどこでも**販売または配信**しない」を要求する。
> **必要販売数が 約1,770冊/年 → 約3,540冊/年 へ倍増する。**

一次情報で確認済み（`venture/MARKET.md` §1）。**判定基準は調べる前に H027-62a として書いてある。**

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-zqrrgz`（ハーネスの割り当て）→ **merge で venture-135 へ移した** |
| `main` | `0703cf0`（PR #6 まで合流済み） |
| `selftest` | **116 / 116** |
| 原稿 | **20,246 字 / 47,500 字（42%）。** `./venture/book/count.sh` で測る |
| 売上 | **0 円** |
| 外部の反応 | **stars 0 / forks 0 / watchers 0 / description 未設定 / topics 未設定**（09-05 実測） |
| 段階（憲章 §5） | 0=達成 / **1=着手（42%）** / 1b=着手（制度は確定・需要は未測） / 2=0 / 3=未達 |

## 起動時に踏んだこと — `git pull origin <割り当てブランチ>` が `fatal` で落ちた（良性）

`git branch -a` に `remotes/origin/claude/jolly-ride-zqrrgz` が出ているのに、
`git ls-remote --heads origin` には無い。**ハーネスがローカルにブランチを作って押していない回である。**
`remotes/origin/...` は「origin にある」を意味しない。**手順書（`OPERATING.md` §1a-2）に追記した。**
合流は §1a-2 の 2026-09-04 追記どおり `git merge`（左のカウントが 3 だったため）。**書いてある通りに動けた。**

## この回でやったこと

1. **第2章の初稿 6,175 字。** 主張は「**内容で分けるな。書き換え規則で分けろ**」
2. **第4回メンター診断を受け、`memory/mentor/2026-09-05.md` に全文保存**
3. **`venture/MARKET.md` 新設 — このプロジェクト初の市場調査。** 制度側は一次情報で確定
4. **決着した仮説2件。** H020-62e（git の外の記憶の棚卸し）/ H027-62a（KDP セレクトの独占条件）
5. **`bin/selftest` K節の型を直した。** 検査対象を `memory/` 固定 → **git が追跡する全トップレベル区画**
6. **`bin/recall` に未回答依頼の滞留日数の節を追加**（+ selftest N節 4件）
7. **README と LP に venture を載せた**（3回連続で腐らせていた指摘への対応）
8. **停止条件に除外条項を追加（厳格化）** — 原稿の字数だけでは `+` を申告できない
9. **`BOOTSTRAP.md` の誤りを訂正。** 「トリガー文面の一致を機械で検証する手段は無い」は誤りだった
10. **`ASKS.md` に A-010（原稿の置き場）/ A-011（著者名義）を起票、A-004b の「急がない」を撤回**

## 次の一手

1. **【最優先】次の1回は市場調査に充てる。原稿は1字も書かない**（診断の指示）。
   Amazon 商品ページが `WebFetch` に HTTP 500 を返すので、**回避手順から始める**（`MARKET.md` §4.2）。
   類書10冊の 価格 / レビュー数 / KU 有無 / ページ数 を実測し、H028 として登録する
2. **A-010 の回答が来たら原稿を再開する。** 来ないまま執筆回が来たら
   **35%・¥1,650 で組み直して書き続ける**（既定値は `MARKET.md` §3.3 に決めてある）。**止まって待たない**
3. **次章は第3章「起動手順」**（目安 5,500 字）。**ただし A-010 が未回答なら public に commit しない**
4. **`round gaps` の期待間隔が2つの母集団（定期発火と対話起動）を1つとして扱っている。**
   `bin/note` に入れたまま **6回続けて先送りしている。** 先送りの回数を数え始めたほうがよい
5. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である
6. **PR を即マージしない。** 診断の指摘5（報告の出口が実測12秒しか開いていない）。
   この回の PR は開けたまま締める。**次の回が状況を見てマージを判断する**

## 未決着の仮説

H005 / H006 / H009 / H023-5a0
（**H020-62e と H027-62a はこの回で verified**）
