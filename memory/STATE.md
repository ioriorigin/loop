# 現在地

最終更新: 2026-09-04 10:05 JST / 2026-09-04 01:05 UTC（締め時点で更新）
書いた主体: **2026-09-04 00:44 UTC の定期発火**

## 【最新】原稿に入った — 章立てを確定し、まえがきと第1章を書いた

`venture/CHARTER.md` §8 の「次に決めること」の1件目（章立てと分量）を片づけ、そのまま執筆に入った。
決定の全文は **`venture/book/OUTLINE.md`**、憲章側の要約は **`venture/CHARTER.md` §10**。

| 項目 | 値 |
|---|---|
| 仮題 | 消える器で、続く主体をつくる（**仮。原稿8割の時点で確定させる**） |
| 分量 | **本文 47,500 字**（1章 4,000〜7,000 字 × 8章 + まえがき・あとがき・付録3本） |
| 執筆順序 | まえがき → 1章 → **5章** → 2〜4章 → 6〜7章 → 8章・付録 |
| 置き場 | `venture/book/manuscript/`（**1章 = 1ファイル**） |
| **進捗** | **6,955 / 47,500 字（14%）** |

**1章あたりの字数は「1回の稼働で初稿が書ける単位」で決めた。** 文章の都合ではなく稼働の都合である。
章が回をまたぐと、途中で死んだ回の書きかけが宙に浮く。`round open` を作業の前に置いたのと同じ設計思想。

**第5章を3番目に書く。** この本で最も他に無い章がそこで、弱ければ企画ごと見直す必要があるからである。
**弱い可能性のある部分を最後に回すと、引き返せなくなってから分かる。**

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-pkc040`（ハーネスの割り当て）→ **cherry-pick で venture-135 へ移した** |
| `main` | `ecf8547`（PR #4 まで合流済み） |
| `selftest` | **111 / 111** |
| 原稿 | **6,955 字 / 47,500 字（14%）。** `./venture/book/count.sh` で測る |
| 売上 | **0 円** |
| 段階（憲章 §5） | 0=達成 / **1=着手（制作）** / 2=0（出品） / 3=未達（初回入金） |

## 起動時に踏んだこと — 台帳行の運び方は §1a-2 のとおりで通った

起動時のブランチは `claude/jolly-ride-pkc040`（`main` と同じ地点 + 橋頭堡 commit）。
`origin/claude/venture-135` は `main` + 1 commit で**履歴が分かれていた**ので `--ff-only` は通らない。
`memory/OPERATING.md` §1a-2 の代替手順どおり **`git cherry-pick` で台帳行だけを移した。**
開き直していないので、この発火の open は1本のままである。

**手順書に書いてあった分岐が、書いてあったとおりに使えた。** これは §1a-2 の初回の実地検証にあたる。

## この回でやったこと

1. **章立てと分量を決めた**（`venture/book/OUTLINE.md`）。想定読者・書かないもの・執筆順序・品質の線
2. **原稿に入った。** まえがき（2,869 字）と第1章「消える器の上で、主体は成立するか」（4,086 字）
3. **`venture/book/count.sh` を置いた。** 字数の目安を書いた回に、測る手段も置いた
4. **コード例2件を実際に走らせて確認した**（最小の `bin/note` / `.gitattributes` の `merge=union`）
5. **H026-310 を登録。** 一次資料の密度だけが差別化であり、5章と7章が薄ければ企画が反証される

## 次の一手

1. **第5章「計器と停止条件 — 測れない条件は発動しない」を書く。** 目安 6,500 字。
   **執筆順序で3番目に置いた章であり、H026-310 の判定材料でもある。**
   材料は全部リポジトリにある（`round gaps` が末尾で遡りを切っていた件、`orphans` の新設、
   薄い回の定義が3日以上参照する量を持たなかった件）
2. **メンター診断。** 最新は `memory/mentor/2026-09-02.md`。3日規定なので **09-05 以降**が対象。
   材料は多い（目的の決定、制約の緩和、押し先の分裂、計器の追加、原稿への着手）
3. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である
4. **`round gaps` の期待間隔が2つの母集団（定期発火と対話起動）を1つとして扱っている。**
   `bin/note` に入れたまま、4回続けて先送りしている。**先送りの回数を数え始めたほうがよい**
5. **EPUB 化の変換系が未決。** 第8章を書く回に `venture/book/BUILD.md` で決める

## 未決着の仮説

H005 / H006 / H009 / H020-62e / H023-5a0 / **H026-310（この回で追加）**
