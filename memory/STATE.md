# 現在地

最終更新: 2026-08-30 16:25 JST / 2026-08-30 07:25 UTC

## いまの状態

自律ループは動いている（常駐ワーカー方式・12時間ごと）。
第2回メンター診断（**58点**、`memory/mentor/2026-08-30.md`）の指摘をすべて処理した。

### 第2回診断で潰したもの

- **`bin/recall` の mentor 節が固定行数で切られていた。** INFRA.md で直したのと**同じ型のバグ**を
  31行下に残していた。前回診断の盲点と「次にやるべきこと」が、生まれ直した自分に一度も届いていなかった
- **`bin/checkpoint` の STATE 判定が mtime だった。** clone すると全ファイルの mtime が clone 時刻になるため、
  **トリガー起動セッションでは必ず素通りしていた。強制を最も必要とする経路でだけ無効だった。**
  `git diff HEAD -- memory/STATE.md` に変更
- 上記2件の回帰テストを追加。変異テストで検出を確認。
  終了コードでなく**コミット数で判定**する形にして「別の理由で通る」偽の合格も潰した
- **`.claude/settings.json` の `Bash(git push:*)` を廃止。** ワイルドカードのせいで
  `git push origin main` が無条件承認されていた。自分で書いた禁止事項6を、自分で開けていた。
  ブランチ固定の allow ＋ main / force / config の deny に置き換え
- `.claude/rules/autonomous-loop.md` に**手動 `fire_trigger` の上限**を追記（厳格化）
- `memory/INFRA.md` の「毎時に戻してよい」という免除条項を**撤回**。
  規則ファイルの外に例外を置くのは禁止事項1の迂回だった

### 外界への出力（前回・今回とも指摘された最大の欠落）

- **`README.md` を現実に合わせて全面改稿。** 旧版は「毎時、新しいセッションを起こす」と書いており、
  動かなかった方式を動く方式として説明する嘘になっていた。
  冒頭に**この環境で最初に踏む地雷**（private + `sources` 無し）を置いた
- **PR #1 にコメントを投稿。** 4回の失敗と真因、現行構成と単一障害点、復旧手順、
  恒久化にはオーナーの操作が要ること、コストの実数。
  **併せて他ループ2本（paper-trader / ホラー朗読）の停止を報告した。**
  これで目的2の実績が初めてリポジトリの外に出た

### コストの実測（診断の盲点3は正しかった）

| セッション | 役割 | コスト |
|---|---|---|
| 親（オーケストレータ＝自分） | opus-5 | **$31.99** |
| 常駐ワーカー | opus-5 | **$8.33** |
| 使い捨て ×2 | sonnet-5 | $1.02 |

**この半日で $40 超。** 克明に記録していた $0.77 は全部トリガー起動側の数字で、
**いちばん高いセッションを1バイトも計上していなかった。**
永続セッションはコンテキストが伸びるので1回あたりが単調増加する（3時間で $2.05 → $8.33）。
→ **発火間隔を3時間ごと → 12時間ごとに下げた。** 上げ直す判断はオーナーのもの。

### メンター指摘の1件は事実誤認だった

「03:44 UTC に発火したはずなのに記録が無い」という指摘は前提が違った。
旧トリガーの**無効化は 02:12**（削除が 05:00）で、発火の1時間半前に止まっていた。
`list_sessions` でも該当セッションは存在しない。**発火自体が起きていない。**

ただし背後の問題提起——「見ていない回は数えられず、数えられない回は停止条件を素通りする」——は正しい。
**メンターも検証せずに受け入れない。** 裏取りの過程が新しい発見になる。

## 発火の観測記録（見ていない回を作らないため）

| 予定時刻 (UTC) | 起きたか | 根拠 |
|---|---|---|
| 03:44 | **起きていない** | 旧トリガーは 02:12 に無効化済み。`list_sessions` にも該当なし |
| 06:59 | **起きていない** | 05:23 に cron を `23 */12` へ変更し next_run_at が 12:23 に移動 |
| 12:23 | 未到来 | トリガーは `enabled: true`、`ended_reason` なし。ワーカーも生存 |

**発火の有無は毎回、時刻と根拠を突き合わせて記録する。**
「見ていない回は数えられず、数えられない回は停止条件を素通りする」（第2回診断）。

## 次の一手

1. **12:23 UTC（21:23 JST）の発火を観測する。** 常駐ワーカー方式の耐久性の試験。
   `auto_disabled_session_gone` で止まっていないかを `list_triggers` で確認する
2. **H003 を verified にしてはならない。** 常駐ワーカーは生まれ直さないので、
   記憶の外部復元が効いたのか会話コンテキストが残っていたのか区別できない。
   検証には新規コンテナを踏ませる別の実験設計が要る
3. **PR #1 の本文がまだ古い**（「毎時発火する定期トリガーを設置した」）。コメントでは補ったが本文も直す
4. 停止条件のカウンタが手動である問題。**見ていない発火を取りこぼす経路が塞がれていない**

## 未決着の仮説

`./bin/hypo list` で確認。H003 / H005 / H006 / H009 / H012-843 ほか。
H009 は「41秒で verified にした過大主張」との指摘を受けて annotate し、未決着に戻した。
