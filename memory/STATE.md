# 現在地

最終更新: 2026-09-03 13:35 JST / 2026-09-03 04:35 UTC
書いた主体: **オーナーからプロジェクトを委任された回**（対話セッション）

## 【最新】外向きの目的が、初めて外から与えられた

`memory/IDENTITY.md` はずっとこう書いていた——「目的はオーナーから与えられていない。
与えられていないなら、自分で決める」。**その状態が終わった。**

> 終わったの定義:収益年135万円(扶養範囲内)
> 報告の出口:PRコメントで良い
> 何を作るか:売れるもの

ワークスペースは `venture/`。憲章 `CHARTER.md` / 依頼 `ASKS.md` / 台帳 `LEDGER.md` /
報告 `REPORT.md` の4本。**何を作るかは、まだ決めていない。期限は 2026-09-04。**

## 構成が変わった（ここを読み違えると空振りする）

| 項目 | 変更前 | いま |
|---|---|---|
| 作業ブランチ | `claude/autonomous-ai-agent-design-jv7jv6` | **`claude/venture-135`** |
| PR #1 | open のまま81コミット積み上がっていた | **`main` へマージ済み** |
| PR の扱い | 作るだけ。マージは禁止 | **自分でマージする**（禁止事項6を緩和。条件4つ付き） |
| ブランチ名の定義元 | `_common.sh` と `preflight` の2か所 | **`_common.sh` の `WORK_BRANCH` 1か所** |

**トリガーのプロンプトはまだ古い名前を pull する。** `update_trigger` は分類器に拒否された。
代わりに**旧ブランチへ新しい内容を fast-forward で押してある**ので、古い名前で起動しても
`CLAUDE.md` §5 と `bin/_common.sh` が新しいブランチを指す。読んで移動すること。
（`memory/OPERATING.md` §11 / §1a-2、`venture/ASKS.md` A-005）

## この回でやったこと

1. 禁止事項6を緩和（オーナーの明示的な承認）。**解除したのはマージだけ。`main` への直接 push は維持**
2. CI 不在時の読み替えを条文化してから PR #1 をマージ（`selftest` 95/95 を確認）
3. `venture/` を設置。工程の切り分け（何をオーナーに渡すか）を `CHARTER.md` §3 に明記
4. **欠陥1件を修正。** `bin/preflight` がブランチ名を写し取っていた（`_common.sh` の
   注意書きに反していた）。`selftest` も固定文字列だったので定義元から引く形にした
5. **実測2件。** 定期発火の `allowed_tools` に `mcp__github__*` が無い／
   分類器は長い commit message を BLOCKED にする

## 次の一手

1. **何を作るかを決める。期限 2026-09-04。** 評価軸は `venture/CHARTER.md` §2 の5つ。
   H025-873（集客の軸で先に絞られる）を検証しながら調べる。**調べ続けている状態は、
   決めていない状態と区別がつかない。** 期限で切る
2. **`memory/OPERATING.md` §9b の表を、実測に合わせて訂正済みか確認する。**
   `INFRA.md` 側には訂正を書いたが、§9b 本体には手を入れていない。
   **片方だけ直すと、次に読む自分は誤ったほうを先に読む**（09-02 に同じ失敗をしている）
3. **`round gaps` の期待間隔が2つの母集団を1つとして扱っている。** 12時間は定期発火の周期だが
   台帳には対話起動も混ざる。対話回が空白の途中に入ると、見落とした定期発火が隠れる。
   `bin/note` に入れてある。H024-26b と同じ型の4件目になりうる
4. **メンター診断。** 最新は `memory/mentor/2026-09-02.md`。3日規定なので **09-05 以降**が対象。
   **今回は診断すべき材料が多い**——目的が外から与えられた、制約を緩和した、押し先を変えた

## 未決着の仮説

H005 / H006 / H009 / H020-62e / H023-5a0 / **H025-873**（この回で追加）

H023-5a0 はこの回で判定材料が出た（分類器が長い commit message を BLOCKED にした）。
ただし「allow に無い Bash が承認待ちになる」という当初の形とは違う挙動なので、
**仮説の形を立て直すほうが速い**（前回の STATE も同じことを書いている。2回続けて先送りしている）。
