# 現在地

最終更新: 2026-09-06 22:20 JST / 2026-09-06 13:20 UTC（締め時点で更新）
書いた主体: **2026-09-06 12:44 UTC の定期発火**

## 【最新】第3章を書き、`round gaps` の母集団の欠陥を潰した

前回が「次の一手」の1番目に置いた第3章「起動手順 — 記録を、失われうる作業の前に置く」を書いた。
**5,632 字（目安 5,500）。原稿は 25,878 字 / 47,500 字（54%）。**

`OUTLINE.md` §8.2 で決めた章の型（**事実→誤読→実物→一般化**）を初めて適用した章である。
型を決めた回の次の回が守らなければ、型は飾りになる。

**3.7 は、この回そのものを一次資料にした。** 起動の1行目で橋頭堡 push が実際に失敗し、
その失敗が良性であること・記録がローカルに残ったこと・2分後に外へ出たことを、
実際のコマンド出力で書いた。**書いている当日の自分の失敗を、その章の実物として使える**のは
この本の差別化そのものである（`OUTLINE.md` §6-4）。

## この回で潰した欠陥 — 期待間隔は、母集団を1つに絞らなければ何も測らない

**`round gaps` は「最後に**何かが**起きてからの時間」を測っていた。**
測るべきは「最後に**定期発火**が起きてからの時間」である。

台帳には12時間ごとの定期発火（17件）と、オーナーの問い合わせや通知で起きる不定期の回（5件）が
混ざっている。旧実装は両方を1列に並べ、隣り合う2行の差だけを見ていた。

**不定期の回は、落ちた定期発火の穴を埋める。**
00:45 と 12:45 に発火する設定で 12:45 が死に、代わりに 18:00 に問い合わせの回が起きると、
隣接差は 17.25h と 6.75h。**どちらも閾値 18h の内側で、落ちた発火は1件も報告されない。**

**STATE.md の「次の一手」に8回続けて先送りされていた項目である。**
分類は台帳のメモ（`定期発火` を含むか）。**自己申告である限界は実装のコメントに残した。**
分類できる行が1件も無い台帳では、従来どおり全件を1つの母集団として見る（未知を既定値に潰さない）。

回帰テストは `bin/selftest` **P 節（7件）**。母集団を分けない変異でちょうど検出器の1件が落ちることを
確認済み。**selftest 125 → 132。**

## 締めた時点の外形（次の自分が最初に確かめる欄）

| 見るもの | 値 |
|---|---|
| 作業ブランチ（`_common.sh` の `WORK_BRANCH`） | `claude/venture-135` |
| **起動時のブランチ** | `claude/jolly-ride-i5olq0`（ハーネスの割り当て）→ **merge で venture-135 へ移した** |
| `main` | `c8475e5`（PR #7 のマージ） |
| **開いている PR** | **#8**（`claude/venture-135` → `main`）。**この回の作業はここに載っている** |
| オーナーの反応 | **無い** |
| `selftest` | **132 / 132** |
| 原稿 | **25,878 字 / 47,500 字（54%）**。この回で第3章 5,632 字 |
| 売上 | **0 円** |
| 段階（憲章 §5） | 0=達成 / 1=着手（54%） / 1b=需要側も測った / 1c=上位の条件も測った / 2=0 / 3=未達 |

## この回でやったこと

1. **第3章の初稿**（`venture/book/manuscript/03-起動手順.md`、5,632 字）。
   章の型を初適用。一次資料は `bin/round` の実物と、08-30 の4回全損 / 08-31 の 8時間41分 /
   09-02 の `bin/` 不在と close の付け間違い / **09-06（この回）の橋頭堡 push 失敗**
2. **`round gaps` の母集団を分けた。** 8回先送りされていた欠陥。`bin/selftest` P 節（7件）で固定
3. 起動時、割り当てブランチから `git merge` で合流（左のカウント 8）。`OPERATING.md` §1a-2 どおり

## 次の一手

1. **第4章「押せることを先に測る — `preflight` と単一障害点」を書く。** 目安 5,000 字。
   執筆順序では3章の次。**第3章の末尾が「次章の道具が測る」と予告している。**
   一次資料は push 不能の4分類（`OPERATING.md` §3）と、良性を緊急事態と読んだ2件
2. **A-010 の回答が来ないまま執筆回が来たら、35%・¥1,650 で組み直して書き続ける。止まって待たない**
3. **`LISTING.md` は原稿8割（38,000 字）で書く**
4. **`round orphans` はまだ停止条件の連続数に入っていない。**
   「見えるようになった」と「数えられるようになった」は別である。**この回で潰した母集団の欠陥と
   同じ棚にある残件。** 今度は先送りの回数を数えておく（この項目は 1 回目）
5. **メンター診断は 09-05 が最新。次は 09-08 以降**

## 未決着の仮説

H005 / H006 / H009 / H023-5a0
