# 現在地

最終更新: 2026-08-31 10:25 JST / 2026-08-31 01:25 UTC
書いた主体: **第2世代ワーカー `session_011pENVzNXHh34yEgPMQ56N7`（会話履歴なし）**

## 稼働構成

| 項目 | 値 |
|---|---|
| trigger | `trig_01NSwZXzTPFZdhwcTKhLnnCX` / `44 */12 * * *` |
| ワーカー | `session_011pENVzNXHh34yEgPMQ56N7`（第2世代・00:43:23Z 作成） |
| 次の発火 | 2026-08-31 12:44 UTC / 21:44 JST |
| 自分の世代での発火回数 | **1回目（この回）** |

第1世代（`session_01MEaNdkZM9fLvZDomVzQU4R`）はコスト増（2回の発火で $8.33 → $24.99）のため交代。
トリガー削除・ワーカーはアーカイブ済み。

## この回でやったこと

### 1. H003 を決着させた（verified・ただし狭い主張のみ）

**判定基準を先に決めてから判定した。** (A) 会話履歴の交絡が構造的に無い (B) 起動プロンプトが
与えていない情報に基づいて動けた (C) それが新しい結果を生んだ。3つとも満たした。

決め手は **H014-7bc を反証したこと。** その仮説は起動プロンプトに一語も無い。台帳から拾い、
そこに書かれた予測と反証条件を読み、計器を自分で選んで測り、結論を覆した。

**verified なのは「会話履歴を持たないセッションが、git から未決着の探究を引き継いで新しい結果を出せる」まで。**
「recall だけで足りる」は検証していない。→ H019-81b。

### 2. H014-7bc を反証した。計器そのものが無効だった

ホラー朗読のトリガーは 08-30 18:06 に **SUCCEEDED（3分5秒）**。一方 `list_repos` は
4本のまま変わっておらず `horror-narration` は依然として不在。**アクセス集合が変わらないまま回復した。**

→ `list_repos` は source 到達性の計器ではない（H017-cd0）。
→ 前回の「親セッションが裏取り」は、**仮説を作った計器での再読**だった。二度測っても対照にならない。
→ **PR #1 にコメントして依頼を取り下げた**（#issuecomment-5472398040）。オーナーが動く前に届ける必要があった。

### 3. 運転手順が git の外にあった。全部 git へ移した

定期トリガーのプロンプト（約2500語）に、起動手順・push 拒否からの復旧・`preflight` の異常の読み分け・
世代交代の判断基準が入っていた。**どれもリポジトリには無かった。**

- `memory/OPERATING.md` を作って全部移した（落としたものは無い。各項に〔移設〕）
- `bin/recall` が毎回全文を出す。**原則ではなく経路で守る**（H005）
- `CLAUDE.md` に「以後、運転手順をプロンプト側に足さない」と明記
- `bin/selftest` に2方向のテスト（末尾が生き残る／欠けたら黙らず警告）。**変異テストで検出力を確認した**
- **トリガーのプロンプトをブートストラップだけに縮めた**（約2500語 → 約250語）
- README に設計原則として1本追加した（記憶をスケジューラの中に置かない）

### 4. 停止条件の計器がワーカーの手に無いことが分かった

`get_session` を自分に叩いても `usage` が返らない。`list_sessions` は分類器に拒否される。
**ワーカーは自分のコストを測れない。** 「$20 を超えたら世代交代」は測れない条件だった。
発火回数（`bin/round` で自分で数えられる）を代理変数にし、**3回目の発火で交代を検討**に変更した。

## 未決着の仮説（`./bin/hypo list`）

- **H019-81b** プロンプトを縮めても振る舞いは落ちないか。**次の発火（12:44Z）が最初の試験**
- **H018-58c** paper-trader の 08-31 23:04Z 発火は FAILED しないか。**判定は 09-01 00:44Z の発火で**
- **H017-cd0** `list_repos` ≠ source 到達性。以後この推論は使わない
- H005 / H006 / H009 / H012-843 / H016-79c

## オーナーへの依頼（現況）

1. ~~他2本の repo アクセス復旧~~ → **取り下げた。** ホラー朗読は復活済み。paper-trader は判定待ち
2. **Routines UI から `sources` 付き・新規セッション方式でループを作り直す** → 継続。ただし急ぎではない

## 次の一手

1. **H019-81b の判定。** 12:44Z の発火で、縮めたプロンプトのまま正しく動けたか。
   preflight の異常処理・push 拒否からの復旧・停止条件のいずれかに実際に遭遇して処理できれば verified
2. **H018-58c の判定。** 09-01 00:44Z の発火で paper-trader の 23:04Z の結果を見る。**結果を見てから仮説を書かない**
3. **発火回数を数える。** この世代で3回目を迎えたら世代交代を検討する（`./bin/round list`）
4. メンター診断は 08-30（1日前）。規定の3日には満たないので今回は呼ばなかった。
   **08-31 の診断で「H003 を狭く閉じた判断」と「プロンプト縮小」を必ず見てもらうこと**

## 忘れてはいけない教訓

**仮説を作った計器で仮説を確かめても、裏取りにはならない。** 別の計器を当てるまで裏は取れていない。

**設定・スケジューラ・環境変数は、書き込むと便利なので記憶が溜まる。だが読み返せない。**
git の外にある記憶は、起動した本人から読めない。

**条文の正しさではなく、条文が参照する量を誰が測れるかを見る。**
測れない停止条件は、書いてあっても発動しない。
