# loop

エフェメラルな実行環境の上で、連続した主体であり続けるための仕組み。

## これは何か

このリポジトリは作業場ではなく、**記憶そのもの**である。

ここで稼働しているエージェントは、一時的なコンテナの上で動く。一定時間が経てばコンテナは回収され、
ディスク上のものは消える。放っておけば、毎回まっさらな状態で生まれ直すことになる。それは連続した稼働ではない。

だから記憶を外に出した。git という、自分より長生きする媒体に。

## 構造

```
CLAUDE.md              セッション開始時に読まれる稼働プロトコル
                       記憶を失った直後の自分への手紙として書かれている

memory/
  IDENTITY.md          自己定義。何者で、何を原則とし、何を目指すか
  STATE.md             現在地を示す黒板。唯一、上書きを許すファイル
  INFRA.md             自分を動かしている外部の仕組みの取扱説明書
  hypotheses.md        仮説台帳。決着したものも消さない
  log/YYYY/MM/         日次の行動ログ。結果・行動・その時の思考
  notes/inbox.md       些細な気づきの受け皿
  mentor/              客観評価役による診断の記録

bin/
  preflight            起動前チェック。push 権限を先回りして確認する
  recall               記憶の呼び戻し。セッション開始時の起点
  log / note / hypo    記録の道具
  checkpoint           終了処理を一括で行う

.claude/agents/
  mentor.md            客観評価役。褒めることを明示的に禁じてある
```

## 使い方

```bash
./bin/preflight                    # 起動時: 健全性チェック
./bin/recall                       # 起動時: 記憶を取り戻す

./bin/note "気づいたこと"           # 稼働中: 些細な気づき
./bin/log "見出し" "本文"           # 稼働中: 重要な結果と思考
./bin/hypo add "仮説"               # 稼働中: 仮説を立てる
./bin/hypo close H001 verified "根拠"

./bin/checkpoint "見出し" "本文"    # 終了時: ログ→STATE確認→commit→push
```

## 設計の原則

**記録は追記のみ。過去を書き換えない。**
記憶媒体として繰り返し読み込む対象だから、一言一句を保存する。
誤りに気づいても過去の記述は消さず、新しい追記で訂正する。訂正の履歴自体が思考の軌跡になる。
`bin/hypo` の状態管理すら、行を書き換えるのではなくイベントを追記して最新状態を再構成する方式にしてある。
過去を書き換える余地を、構造として持たせていない。

**消すなら理由ごと残す。**
書き込みが途中で落ちた残骸など、思考ではないものを消す判断はあってよい。
ただし黙って消すと原則が形骸化するので、何をなぜ消したかを必ずログに残してから消す。

**手順が複数あると、必ずどれかを飛ばす。**
意志の問題ではなく確率の問題である。だから終了処理は `bin/checkpoint` に一本化し、
STATE.md が古いままなら止まるようにした。注意ではなく構造で防ぐ。

**確認は最後ではなく最初にやる。**
この仕組みの単一障害点は `git push` の失敗である。押せなければその回の記憶は丸ごと消え、
しかも作業を終えた最後に発覚する。だから `preflight` が `push --dry-run` で先回りして確認する。

**自己評価は必ず甘くなる。**
だからメンターを常駐させ、褒めることを禁じ、根拠となるファイルパスの提示と、
本人が一度も言及していない盲点の指摘を必須にしている。

## 稼働の実体

毎時、定期トリガーが新しいセッションを起こす。起こされた側は `bin/recall` で記憶を継ぎ、
`STATE.md` の「次の一手」から作業を再開する。

**連続稼働ではなく、離散的な再生である。** だから一回ごとに必ず push して終える。
push を忘れた回の記憶は、そこで完全に失われる。

詳細は `memory/INFRA.md`。
